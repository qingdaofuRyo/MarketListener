# 正式开发交接

> **归档说明（2026-08-24 更新）**：本文件保存正式计划固化前的交接基线。当前执行入口为根目录 [`Plan_R3.md`](../Plan_R3.md)，整体架构见 [`ARCHITECTURE.md`](ARCHITECTURE.md)。下文 Day 0 事实保持历史原貌，不构成当前待办。

## 当前决定

用户于 2026-08-04 决定停止执行 `Plan.md` 的 Day 0 任务。Day 0 没有封板，也不再作为后续会话的自动执行队列；其代码、测试、报告和失败证据保留为整个项目正式开发的基线。

新会话应以完整项目目标开展工作，而不是继续某个 `D0-*` 编号。任何架构、契约或数据边界调整仍以 `ADR.md` 的规则为准。

## 已验证基线

- 桌面端：`desktop\\.venv\\Scripts\\python.exe -m pytest desktop\\tests -q` 实际通过，结果为 `49 passed`。
- Android：`android\\gradlew.bat -p android testDebugUnitTest assembleDebug --no-daemon` 实际通过。
- 16 KB：Debug APK 经 `zipalign -c -P 16` 检查通过；四种 ABI 的 SQLCipher `PT_LOAD` 均为 `0x4000`；APK 已在 16 KB 模拟器启动，中文未导入状态可见。
- Android 已具备已激活行情包的只读查询和离线 K 线转换代码，但尚未以真实 Provider 生成的数据包完成验收。

## 不可当作完成的事项

- 没有 Provider 取得真实 `PASS`：聚宽缺本机凭据，Baostock 超时，AkShare 网络/代理失败，`tdx_quant` 未验证支持。
- 没有真实签名行情包，因此真实导入、日线/分钟线、离线查询和篡改回滚未验收。
- Chaquopy + NumPy 可离线打包，但在 16 KB 设备加载 `chaquopy-libgfortran` 失败；试验代码已移除，不能把桌面 NumPy、构建成功或 4 KB 设备结果视为策略验收。
- Git 仓库没有已跟踪基线，所有项目文件目前显示为未跟踪；未执行 `git add`、初始化或提交。

## 新会话启动顺序

1. 阅读 `README.md`、`ADR.md`、`CONTEXT.md`、`行情监控和产业链图谱项目.md`、本文件、`Experience.md` 和 `Log.md`。
2. 将 `Plan.md` 和 `docs/deliveries/` 作为 Day 0 历史证据阅读，不自动继续其中任何任务。
3. 与用户确认正式开发的首个项目级目标、验收标准、数据源授权/凭据方案和版本控制基线。
4. 若要复用 Day 0 的实现或重新挑战其阻塞项，先制定新的任务和真实验收条件；不得把历史失败记录改写为成功。

## 文档状态

- `ADR.md`、`docs/adr/` 和 `CONTEXT.md`：现行规范，未因停止 Day 0 而修改。
- `Plan.md`：Day 0 归档计划，任务和验收要求保留原文。
- `docs/deliveries/`：历史交付与证据，索引见 `docs/deliveries/README.md`。
- `docs/Agent任务交付模板.md`、`docs/Day0验收报告模板.md`：模板，不是当前项目状态。
