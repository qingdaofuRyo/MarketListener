# R4 第四轮开发计划（当前唯一活动计划）

最后更新：2026-09-06。本文承接 `Plan_R3.md`，围绕网页端“数据源、数据、国内期货数据、行情、策略”页面及同轮次确认的交互改进滚动收敛第四轮需求；当前已登记国内期货数据页、80 色画线调色板、笔刷画线、品种/席位的市值与持仓四张结构图、中国商品期货多空热度体系、网页端“数据”页面三分区、行情分类治理，以及 Indicator / Strategy Function / Strategy 三层策略系统和标的详情图表交互改版。R1、R2、R3 和 `docs/STATUS.md` 只保留历史事实与未完成项来源，不再新增平行待办。

## 状态与执行原则

- 状态使用 `NEW → ANALYSIS → PLAN_CREATED → CODING → CODE_REVIEW → TESTING → VERIFYING → DONE`；外部数据、授权或用户决策无法由代码解除时使用 `BLOCKED`。
- 每条 R4 需求先锁定术语、数据来源、计算口径、接口、缺失数据行为和验收样本；仍有公式决策时保持 `ANALYSIS`，不得进入实现。
- 参考站点 `jiaoyikecha.com` 与 `x.qhkch.com` 只用于信息架构和交互研究；不得复制其受保护内容、绕过登录/付费限制或把未经授权的网页接口当作正式数据源。
- 页面只展示可追溯的本地标准数据和派生结果。所有派生值必须保留输入覆盖、来源、交易日、公式版本、计算时间和不可用原因。
- R3 未完成的数据正确性问题继续有效；创建 R4 不代表 R3-T002、R3-T008、R3-T009 等缺口自动完成。

## 当前代码基线

- 网页端已有独立 `/futures/` 路由与 `FuturesView.vue`：多空热度、品种持仓结构、席位持仓质量门、商品合约多轴容器和具体月份合约席位表均只请求本机 FastAPI。
- `futures_dashboard.py` 已能按主力/加权/合约/指数序列计算每日涨跌家数，但采集任务目前只正式生成主力品种宽度。
- `futures.py` 已定义沉淀资金公式：`持仓量 × 2 ×（最新价 × 交易乘数 × 保证金率）`，并保留合约规格生效日和公式版本；真实全合约覆盖仍需验证。这里的交易乘数为 `交易单位 ÷ 报价单位`，与名义持仓规模的价格基准分开。
- `FUTURES_OI_LEADERBOARD` 保留历史兼容聚合；原固定 `RB2610 / 2026-08-07` 样例已删除，`FUTURES_MEMBER_POSITION_DAILY` 逐所保存公开多/空方向排名。2026-08-28 的 DCE 端点解析失败，商品席位覆盖尚不完整。
- `docs/DATA_SOURCE_CAPABILITY_MATRIX.md` 对沉淀资金仍保留旧的 `BLOCKED` 结论；只有完成来源、覆盖和真实样本复核后才能更新该结论。
- 当前没有现货/基差标准数据集；已按全月份合约生产品种持仓日度结构及固定基准，席位持仓原始排名与部分覆盖日度结构也已落库，但不完整交易所覆盖不会建立正式席位基准。
- 本地 Silver 已有按周期/年份分区的国内期货 Parquet，月份合约日线可提供价格、结算价和 `open_interest`；但目录同时包含连续、加权和指数序列，进入结构计算前必须先按“有效月份合约”规则去重过滤并完成真实覆盖审计。
- 2026-08-27 只读审计确认：本地 2026-08-21 商品期货月份合约有 725 条、73 个品种，结算价与持仓量可用；合约乘数和历史保证金率只有 3 个品种可计算。品种热度可先形成，资金热度和总热度在覆盖门槛通过前必须返回不可用，不能用局部品种冒充全市场。
- `R4-T008` 随后新增逐交易日期货规则快照并完成真实回算：2026-08-21 方向覆盖 `74/74`，资金覆盖 `73/74 = 98.65%`，已超过集中配置的 `80%` 门槛；剩余 1 个品种缺口继续以 `PARTIAL` 暴露，不改写为完整覆盖。
- Web 已锁定 ECharts 6 并有折线、排名、热力图和 K 线组件，可复用实例生命周期、缩放和响应式基线；当前通用组件没有固定顺序多系列堆叠、“其他”集合下钻或相邻色对比规则。
- 行情详情画线类型目前只有 `horizontal / vertical / rectangle / text`；笔刷需要新增多点折线模型、连续指针手势和折线命中选择。后端当前把画线实例保存为透明 JSON 文档，扩展 `brush` 不需要改写历史数据。
- 2026-08-26 对两个参考站点的公开页面读取均未成功，因此本计划只采用用户明确提供的信息架构，不声称复刻了参考站交互。

## 已确认的 R4 领域决定

- 用户所说“持仓品牌”统一更正为“持仓品种”。
- `FUTURES_BREADTH` 的全月份合约/主力序列涨跌家数保留为历史兼容指标；新的多空热度先形成品种级结算收益，再统计上涨、下跌和平盘品种，不静默改写旧合约广度。
- 多空热度默认排除中金所，使用最近 10 个有效交易日、半衰期 3 个交易日的指数衰减；底层保存品种热度和资金热度，总热度按用户权重动态生成，默认品种 40%、资金 60%。
- 品种级沉淀资金是同一交易所品种下所有有效月份合约沉淀资金之和，复用现有单边持仓量推算双边保证金的版本化公式；资金字段覆盖低于 80% 时资金热度和总热度不可用。
- 四张“市值/持仓分布”统一使用固定顺序堆叠面积图，不再按普通多折线实现；每个指标和席位方向分别维护自己的固定排序及“其他”成员集合。
- “市值分布”中的业务量统一定义为**名义持仓规模**，与“沉淀资金”分开：合约值为 `结算价 × 交易乘数 × 交易所单边持仓量`，其中交易乘数为 `交易单位 ÷ 报价单位`；不乘保证金率，也不额外乘 `2`。`priceBasis` 固定为 `SETTLEMENT`，品种市值分布及后续席位市值分布不得改用收盘价或逐行混用。页面标题保留用户熟悉的“市值分布”，图注和接口必须展示真实公式名称。
- 四张结构图默认只统计商品期货，排除中金所金融期货；若以后开放金融期货，必须使用独立筛选和独立基准元数据，不得改写既有结构历史。

## R4 任务

### R4-T038～R4-T044 — 图表工具与四类信号监控（2026-09-06）

状态：`VERIFYING`（代码已完成，等待用户实际体验）。本节覆盖用户新确认的七项要求；T035 的价格警报和策略页旧回测操作以本节为准退出现行界面，历史实现证据保留。

- **T038**：左栏改为可展开的工具组：线条；箱体/Fibonacci/多头盈亏比/空头盈亏比；笔刷/激光笔；文字。盈亏比是用户画线，三个价格锚点表示开仓参考价、止损参考价、目标参考价，展示风险收益比，不创建策略或订单。
- **T039**：图表类型仅显示图标，其他功能增加图标；指标按钮不显示实例数；移除警报界面与轮询。回放底部图标工具栏提供图上选起点、播放/暂停、倍速、单步/快进及退出。
- **T040/T041**：策略页只面向开仓/加仓/减仓/平仓信号，无默认示例、市场类型、仓位、资金、止损止盈、滑点、回测/归档/复制/历史版本/加载图表操作；提供创建、编辑、启停和可恢复删除。周期统一为 5m/15m/30m/1h/2h/1d/1w/1mo/3mo/1y。旧资源与回测记录保留，不自动改写成新信号或删除用户数据。
- **监控规则（用户已确认）**：只有开仓策略参与市场筛选；开仓信号在同一标的、同一多空方向建立监控，后续启用所有已启用的同向加仓/减仓/平仓策略。后续信号必须晚于开仓信号；同一时点平仓优先，触发后结束本轮，不能由同根开仓信号重启。刷新/重复扫描不重复处理同一策略版本和 bar；更新本地行情后才有新的监控判断，不承诺实时账户连接或实盘成交。缺数据/暖机不足显示原因，不补零。
- **T042**：修复国内期货指数中文简称，按交易所/源市场/代码查找已有本地名称表，补齐已验证映射；不得凭裸代码跨市场猜名字或重写 Silver。
- **T043**：报价恢复振幅、涨跌、成交额、持仓量、沉淀资金、总/流通市值。后三项取最新快照，不随悬浮变化；报价行不显示时间，分钟轴显示时分，日线以上只显示日期；按字段分色。指标图例直接提供跨周期、颜色、显隐和删除，更多仅保留参数等。
- **T044**：卡片/列表图复用无时间彩色报价，周期改为下拉选择。

验证限于相关 Python、Vue 构建及桌面浏览器场景；不全量验证，不开发移动端，不扩大审计或推送。

交付记录（2026-09-06）：

- 分组图标工具栏、三价格多空盈亏比、直接指标图例控件、无日期彩色报价和卡片/列表周期下拉均已接入。回放提供图上点选起点、0.5/1/2/4/8 倍速、单步、快进 10 根与退出；只在当前已加载窗口内回放。
- 新 `SignalStrategyManager` / `/api/signals` 与历史回测资源隔离，当前策略页不再载入原先默认列表；旧 custom 定义及回测文件完整保留，未物理删除。信号删除为 tombstone，可在页面撤销，API 可恢复；停止/删除一条开仓策略不冒充平仓、不移除已有监控轮次。
- 首次检查只判断最新已结束 bar，后续扫描按策略版本和 bar 去重。开仓筛选由用户点击“检查开仓信号”启动；已监控标的在行情页打开时每 30 秒检查本地新增数据，也可手动更新。扫描分批显示进度、可停止；监控分页使用标的游标避免平仓移出集合后跳过后续标的。检测到超过 500 根的历史缺口时显示错误并保留游标，不伪装完整补算。无实时连接，无后台常驻或真实成交承诺。
- `tdx_futures_index_names.json` 从期货通 `ds_stk.dat` 的 42/47/68 来源域提取交易所+代码名称；来源 SHA-256：`4FC7F04B49AEFB16502379EF99BBABA5B6BDC925341912F45E62059518E3C5E8`。只读检查当前“国内期货-指数”128 个标的，修复后名称包含原始代码的占位项为 0；展示缓存版本提升为 `market-categories-r4-v4`，不改写 Silver。
- `npm run build`、相关 Ruff 通过；`pytest desktop/tests/test_signal_monitor.py desktop/tests/test_strategy_definition.py -q` 为 16 项通过；两个新图表/信号桌面 Playwright 文件共 7 项通过（24.8 秒），覆盖图表工具、回放、报价、四类策略 CRUD/恢复、开仓筛选和后续信号标记。浏览器使用确定性夹具，不是对全部市场策略的真实扫描验收。
- 截图：`desktop/web/test-results/r4-chart-workbench.png`、`r4-indicator-library.png`、`r4-signal-strategies.png`；源码、测试与文档直接修改本地仓库，未提交/推送。本次没有运行全量或 Android 验证，没有新增移动端设计。

### R4-T032～R4-T037 — 标的详情图表交互改版（2026-09-06）

用户六项新增需求统一纳入第四轮，按 T032/T033 → T034 → T035 → T036/T037 开发；当前状态 `VERIFYING`（代码与定向验证完成，等待用户查看实际效果）。参考 TradingView 超级图表、Excalidraw 激光笔和同花顺行情的信息架构，全部行情仍来自本地 API。参考页面动态内容未能由网页读取工具完整展示，不宣称像素级复刻。

- **T032 / P0：左侧画线工具栏与激光笔。** 工具按线条、图形、注释组织；保留既有画线与样式。增加临时激光笔，连续轨迹淡出，不写入画线文件，退出/切换后清理。
- **T033 / P0：自由笔刷修复。** 自由轨迹不吸附 OHLC、不取整到 bar 中心，持久化时间/价格逻辑坐标；缩放及刷新后形状保持。定向测试验证斜向/曲线和已开启吸附的场景。
- **T034 / P0：指标大弹窗与悬浮图例。** 顶部指标按钮打开居中大弹窗；统一策略指标目录、搜索、主/副图分类、收藏星标；点名称直接添加。图表左上方显示实例名、主副图标识、颜色显隐、删除和更多设置；设置含跨周期、参数、颜色、线宽及线型。旧实例默认跨周期，收藏复用策略页键。
- **T035 / P1：图表类型、警报与回放。** 实心、空心、平均 K 线、折线、面积图可切换并保存偏好。平均 K 线仅用于显示，不替换原始行情及策略输入。价格警报保存到本机浏览器，页面运行期间定时检查本地最新 bar，仅在满足条件时触发一次并可重新启用；明确页面关闭时不监测。回放支持选起点、播放/暂停、单步、速度和退出；隐藏未来 K 线/指标/策略报告及画线，指标计算请求限制到当前回放点，不将回放价格用于真实警报。
- **T036 / P0：紧凑图表布局。** 名称、代码、OHLC 等合并为一行；顶部左侧依次图表类型、指标、涨跌换色、坐标翻转、警报、回放；周期栏固定底部。“坐标反转”统一改名“坐标翻转”。
- **T037 / P0：Escape 退出。** 有模态弹窗/文本编辑时先关闭或取消当前操作，普通详情状态按 Esc 返回行情列表并停止回放。

验证：Vue 构建和相关桌面 Playwright，重点覆盖轨迹、临时激光笔无保存、指标收藏/添加/设置、五种类型、警报去重、回放数据边界、布局与 Esc。按用户约束不运行全量验证、不新增移动端设计，不持续扩大审计；开发与文档更新后交由用户查看实际效果。

2026-09-06 交付证据：

- T032/T033：左栏分组及两点趋势线已实现；自由笔刷在绘制和移动时保留连续时间/价格坐标并绕过 OHLC 吸附。激光笔使用独立 Canvas，轨迹约 1.2 秒淡出，不进入画线持久化。
- T034：居中 860px 指标库复用策略目录及收藏；搜索、主副图筛选、点名称添加、悬浮图例、显隐/删除/参数/颜色/跨周期均已接入。跨周期关闭只影响当前实例展示，不修改指标定义。
- T035：五种类型已接入并保存偏好；平均 K 线只变换显示值，报价及指标输入保留原始值。回放针对当前已加载行情窗口，可拖进度、单步、1/2/4 倍播放及退出；每步裁切 bars 和指标请求，回放中隐藏策略结果及持久画线，禁止新增持久画线但允许激光笔。警报仅在当前标的详情打开时每 15 秒读取最新本地 bar；满足条件后停用，可手动重新启用；页面关闭不监测，无系统通知或后台推送承诺。
- T036/T037：顶部功能行 + 紧凑报价行，周期置底，按钮改名“坐标翻转”；Esc 先处理指标/警报弹窗及编辑焦点，普通状态退出详情并停止播放。
- 实际执行 `cd desktop/web; npm run build` 通过（保留既有大包体积警告）；`npm run test:e2e -- e2e/chart-workbench-r4.spec.ts e2e/terminal.spec.ts e2e/market-drawing-preferences.spec.ts --grep "chart presentation|brush preserves|indicator library|trend line uses|strategy indicators load|brush drawing saves|rectangle drawing previews|fibonacci retracement"`：`8 passed (24.2s)`。全部为桌面定向场景，合成行情用于确定性验证，不代表真实 Provider 验收。
- 截图位于忽略目录 `desktop/web/test-results/r4-chart-workbench.png`、`r4-indicator-library.png`。本次修改直接位于 `C:\Users\qingd\Documents\MarketListener`，未提交或推送；不运行全量、Android 或移动端设计，后续以用户实际体验反馈为准。

### R4-T001 — 网页端国内期货数据页面

- `type`：期货数据工程与网页产品；`priority`：P0；`state`：VERIFYING；`failure_count`：0。
- `目标`：新增独立 `/futures/` 页面，以全月份合约和交易所公开席位排名为可追溯计算底座，完整承载本轮确认的七个模块：多空热度、品种市值分布、席位市值分布、品种持仓分布、席位持仓分布、商品合约和商品合约席位分布。
- `范围边界`：参考站点只提供信息架构启发。页面不请求参考站点，不拷贝其数据/文案，不使用未经验证的隐藏接口。
- `实现证据（2026-08-29、2026-09-01）`：`/futures/` 已实现多空热度、品种持仓固定堆叠、席位持仓质量门、商品合约席位排名与商品合约多轴图。`GET /api/futures/contracts` 仅列出本地 Silver 中的商品期货月份/原生加权序列；`GET /api/futures/contract-series` 返回真实日线 OHLC 与单边持仓量。用户已锁定统一 `priceBasis=SETTLEMENT` 后，具体月份合约的名义持仓规模使用 `结算价 × 同交易日交易乘数 × 单边持仓量`；缺少任一输入保持 `null`，加权序列仍不会以自身持仓量伪造名义规模。基差仍因现货口径/来源未决返回明确不可用原因。相关 API 单测与页面交互定向验证通过，仍待席位来源和完整验收。

#### 页面信息架构

1. **多空热度**：使用 3 个半圆 Gauge 展示总多空、品种多空和资金多空热度，并用同一张三线历史图展示每日 10 交易日指数衰减结果；总热度支持用户权重滑块和长期持久化。完整规则见 `R4-T008`。
2. **品种市值分布**：按“交易所+品种”汇总该交易日全部有效月份合约的名义持仓规模，使用固定顺序堆叠面积图展示市场结构；完整规则见 `R4-T004`。
3. **席位市值分布**：按席位汇总全部已公布的商品期货品种和合约名义持仓规模，使用固定顺序堆叠面积图展示公布排名覆盖范围内的席位结构；完整规则见 `R4-T005`。
4. **品种持仓分布**：按“交易所+品种+交易日”汇总全部有效月份合约的单边持仓量，使用固定顺序堆叠面积图展示品种结构；完整规则见 `R4-T006`。
5. **席位持仓分布**：按“席位+交易日”汇总全部已公布的商品期货合约持仓，使用固定顺序堆叠面积图展示席位结构，并切换多头、空头、毛持仓、多头净持仓和空头净持仓口径；完整规则见 `R4-T007`。
6. **商品合约**：选择交易所、品种后，查询某个具体月份合约或该品种加权合约。同一图表容器展示价格/K 线、持仓量、市值和基差四类序列，四类指标颜色与图例固定可辨；页面显示各轴单位、数据日期和不可用原因。
7. **商品合约席位分布**：对某个月份合约或品种加权口径，查询席位多头持仓、空头持仓、多头净持仓和空头净持仓；支持交易日、席位和方向筛选，保留持仓增减、来源、覆盖名次以及品种/席位双向反查。

#### 已锁定的计算边界

- “有效月份合约”是该交易日存在真实日线、尚未到期且属于国内期货交易所的具体交割合约；排除主连、次连、加权、商品指数、期权和已经到期的合约。
- 旧合约广度的方向继续使用本合约收盘价与上一有效交易日收盘价比较；新多空热度使用 `R4-T008` 的品种级结算收益方向，两种指标不得混用。
- 合约沉淀资金沿用双边保证金估算公式；缺少持仓量、价格、合约乘数或当日有效保证金比例时保持 `NULL`，不得补零。
- 多空热度的上涨资金、下跌资金和平盘资金按品种方向汇总该品种全部可计算月份合约沉淀资金；品种方向覆盖与资金覆盖必须分别返回。
- 席位已经分别给出多头和空头持仓，因此席位多头/空头资金按各自持仓量计算，不再乘双边系数；同时返回毛持仓资金与净方向资金，禁止把净持仓绝对值冒充实际占用保证金。
- 多空热度窗口按交易日而不是自然日取最近 10 个有效日；不足 10 日时按已有交易日重新归一化权重并返回 `isWarmup=true`，不用缺失日补零。
- 品种持仓量以交易所公布的单边 `open_interest` 按有效月份合约相加；不把加权连续合约的持仓量再加一次。
- 席位标识的默认主键是 `exchange + normalized_member_name`。在没有经审计的别名表前，不将不同交易所的同名字符串自动合并成同一席位。
- 品种加权价格/K 线沿用已有持仓量加权算法，同时返回当日成分合约和权重；加权口径的持仓与席位值仍来自有效月份合约汇总，不用连续合约伪造席位记录。
- 四张结构图由 API 返回所有真实成员及固定“其他”集合，不在 API 层静默截断为 Top-N；主图只按固定元数据合并“其他”，点击后必须能下钻查看其中全部真实成员。

#### 待用户继续锁定的公式决定

- “席位结构”是否还要维护外资、产业、机构等人工分类；当前只能可靠定义交易所会员席位，未授权自动贴机构类型标签。
- 商品合约的“K 线与另外三条折线用四种颜色”存在表达冲突：K 线本身需要涨/跌两色。需确认保留蜡烛 K 线并叠加三条线，还是将 K 线简化为“收盘价线”与另外三条线同图。
- 价格、持仓量、市值和基差量纲不同；建议在同一图表容器中使用独立多 Y 轴，而不是将原始值硬画在同一轴上。若用户更偏好归一化指数，需另行确认基期和缺失值规则。
- 基差的方向（现货-期货或期货-现货）、现货品牌/地区选择、价格单位转换和加权合约基差计算尚未确认；没有通过真实探针的现货源时基差必须显示不可用。

#### 计划新增或调整的数据接口

- `GET /api/futures/overview?trading_day=`：市场总览、数据日期、合约数量及覆盖状态。
- `GET /api/futures/heat?start_day=&end_day=`：返回日度品种/资金原始分数、10 日指数衰减结果、上涨/下跌/平盘品种与沉淀资金、覆盖、预热和集中配置；不保存或返回唯一固定权重的总热度历史真值。
- `GET /api/futures/structures/{chart_id}?direction=&range=1y|3y|5y|all&start_day=&end_day=&level=main|other`：四张固定顺序堆叠面积图所需的最小时间序列、合计值、固定基准元数据、覆盖和来源；`chart_id` 分别为 `product-notional`、`member-notional`、`product-open-interest`、`member-open-interest`。
- `GET /api/futures/contracts?trading_day=&exchange=&product=`：返回可选月份合约、加权合约、有效日期和质量状态，供联动筛选使用。
- `GET /api/futures/contract-series?exchange=&product=&contract=&series_kind=CONTRACT|WEIGHTED&start_day=&end_day=`：同时返回 OHLC、持仓量、市值、基差、单位、成分合约/权重和逐日不可用原因。
- `GET /api/futures/contract-members?trading_day=&exchange=&product=&contract=&series_kind=CONTRACT|WEIGHTED&member=&direction=&page=&page_size=`：某月份合约或品种加权口径的多头、空头、多头净持仓、空头净持仓与增减。
- 接口字段使用 camelCase；所有金额同时返回原始人民币数值和页面显示单位，不在 API 中只返回格式化字符串。
- 时间序列以交易日对齐，缺失值返回 `null`，不在 API 端用前值填充；席位列表接口分页，分布图接口不用分页逼使图表丢线。

#### 数据集与来源计划

- 新增 `FUTURES_CONTRACT_DAILY_SNAPSHOT`：主键为来源+交易所+合约+交易日，保留 OHLC/结算价、持仓量、合约乘数、保证金率、合约名义持仓规模、沉淀资金、公式版本、覆盖和计算原因。
- 新增 `FUTURES_HEAT_DAILY`：保留日度品种/资金分量、10 日窗口、指数衰减参数、品种/资金原始分数、两个 10 日结果、背离、覆盖率、预热、质量状态和公式版本；总热度按用户权重即时线性组合，不作为唯一 Gold 真值。
- 新增 `FUTURES_MEMBER_POSITION_DAILY`：保留交易所原始席位名、规范名、品种、合约、多/空持仓及增减、排名、公布覆盖、来源和获取时间。
- 新增可重建的 `FUTURES_STRUCTURE_DAILY` Gold 数据集，以及持久化业务元数据 `FUTURES_STRUCTURE_BASELINE`；后者按图表、指标和方向保存基准交易日、固定顺序、主图成员、“其他”成员、阈值和版本。保留现有 `FUTURES_BREADTH` 与 `FUTURES_OI_LEADERBOARD` 作为历史兼容数据，不静默改变其旧口径。
- 基差单独建立现货价观测和 `FUTURES_BASIS_DAILY` 派生数据集；现货价格、期货价格与单位转换各自可追溯，不将来源不同或规格不匹配的值强行相减。
- 月份合约行情优先使用通达信期货通本地 `.day`，持仓席位优先使用交易所公开排名或经真实探针验证的 AKShare 对应接口；不同来源物理隔离，不逐字段混拼。
- 合约乘数和保证金率必须按交易日解析生效版本；来源缺少历史保证金率时，该日资金不可用，不能套用当前比例回算历史。
- 将当前固定螺纹钢样例改为按交易日发现有效合约的受控采集任务；单个交易所或合约失败记为部分失败，不擦除其他已成功结果。
- 六个国内交易场所（上期所、能源中心、大商所、郑商所、中金所、广期所）的行情与席位能力分开探针；某一场所成功不推断其他场所可用。

#### 验收标准

- 使用至少 12 个交易日、多个交易所、两个以上月份合约和多个重复席位的确定性夹具，验证品种/合约方向分离、品种聚合、10 日指数衰减、合约到期、无前结算、缺少规格、低覆盖、市值/沉淀资金分离、席位跨合约汇总、多空净持仓、基差单位不匹配和稀疏日历。
- 对每个国内期货交易所至少选择一个真实品种，保存可复现探针的日期、合约数、席位行数、字段、来源和质量状态；测试夹具通过不能替代真实数据验收。
- Vue build 和 Playwright 覆盖 `/futures/` 导航、空状态、部分覆盖提示、三 Gauge、10 日热度、权重持久化、三线历史联动、四个固定堆叠结构模块、图例与图层悬浮、固定“其他”下钻、月份/加权切换、四指标同图、品种/席位双向筛选、分页及后端错误降级。
- 数据量验收至少包含 100 个品种系列和 500 个席位系列的合成性能样本；缩放、图例搜索、悬浮高亮和“其他”下钻无明显卡顿，不通过不可追溯的截断来达标。
- 页面不得请求参考站点或第三方 Provider；所有访问经本机 FastAPI，加载失败时显示数据日期、失败来源和重试入口，不显示模拟值。
- 数据集登记、API 契约、能力矩阵、架构、经验、日志和 R4 状态随实现同步更新；全量 Ruff、pytest、Vue、Playwright 和适用 Android 回归通过后才能进入 `DONE`。

### R4-T002 — 画线工具 80 色预设调色板与自定义透明度

- `type`：网页端画线交互；`priority`：P1；`state`：VERIFYING；`failure_count`：0。
- `目标`：将水平线、垂直线、箱体线和文本框的线条/文字颜色，以及箱体填充颜色选择器，从当前 24 色升级为附图所示的 80 色预设调色板；在色板下方提供自定义颜色入口和可感知当前 Alpha 的不透明度控件。
- `范围边界`：只调整画线工具的 `DrawingColorPicker` 及其预设颜色常量、样式持久化和测试；不改变行情涨跌色、图表主题色、行标记色板、数据页图表颜色或后端画线 Schema。

#### 名称与视觉契约

- 整体组件统一称为“画线颜色选择器（Drawing Color Picker）”；上方 80 色区域称为“预设调色板（Preset Color Palette）”；每个方块称为“色块（Color Swatch）”；下方入口称为“自定义颜色选择器（Custom Color Picker）”。
- 桌面端预设调色板固定为 `10 列 × 8 行`、共 80 个色块：第一行为从白到黑的灰阶，第二行为高辨识度基础色，第三至第八行为对应色系由浅至深的层级。不得按明暗主题动态改写这些值，确保已保存颜色长期稳定。
- 根据本次参考图片读取并锁定以下有序色值；实现时放入单一 TypeScript 常量，测试同时锁定总数、顺序和首尾值：

```text
#FFFFFF #DBDBDB #B8B8B8 #9C9C9C #808080 #636363 #4A4A4A #2E2E2E #0F0F0F #000000
#F23645 #FF9800 #FFEB3B #4CAF50 #089981 #00BCD4 #2962FF #673AB7 #9C27B0 #E91E63
#FCCBCD #FFE0B2 #FFF9C4 #C8E6C9 #ACE5DC #B2EBF2 #BBD9FB #D1C4E9 #E1BEE7 #F8BBD0
#FAA1A4 #FFCC80 #FFF59D #A5D6A7 #70CCBD #80DEEA #90BFF9 #B39DDB #CE93D8 #F48FB1
#F77C80 #FFB74D #FFF176 #81C784 #42BDA8 #4DD0E1 #5B9CF6 #9575CD #BA68C8 #F06292
#F7525F #FFA726 #FFEE58 #66BB6A #22AB94 #26C6DA #3179F5 #7E57C2 #AB47BC #EC407A
#B22833 #F57C00 #FBC02D #388E3C #056656 #0097A7 #1848CC #512DA8 #7B1FA2 #C2185B
#801922 #E65100 #F57F17 #1B5E20 #00332A #006064 #0C3299 #311B92 #4A148C #880E4F
```

- 当前色块必须有不依赖颜色本身的选中边框或勾选态；白色、黑色和完全透明颜色也必须能看清选择状态。
- 浅色和深色主题下均保持与附图相同的信息层级；允许响应式缩放色块或面板宽度，但不得少色、改变顺序或退化为横向难以浏览的单行列表。

#### 交互与持久化规则

- 线条颜色选择器用于水平线、垂直线和箱体边框；文本框使用同一组件设置文字颜色；箱体填充颜色使用同一 80 色表并显示棋盘格透明背景。
- 点击预设色块只替换 RGB，保留进入选择器时的当前 Alpha；线条色、文字色和填充色都允许分别调整 `0%–100%` 不透明度，步长为 1%。
- 色板下方显示“当前颜色色块 + 添加符号”；点击后打开浏览器可用的自定义颜色选择器。自定义颜色立即应用于当前图形，但不插入或覆盖固定 80 色表；本轮不增加收藏色、最近使用颜色或云端同步。
- Alpha 滑块使用透明棋盘底叠加“透明 → 当前 RGB”的渐变；拖动圆点填充色必须是当前 RGBA，圆点自身保持清晰边框和阴影，旁边实时显示整数百分比。即使 Alpha 为 0，拖动圆点仍必须可见、可聚焦。
- 输出继续归一化为 `rgba(r,g,b,a)`；选择色块、自定义颜色或调整 Alpha 后，画布实例、悬浮工具栏预览和浏览器持久偏好必须同步更新。
- 沿用当前按 `horizontal`、`vertical`、`rectangle`、`text` 分类型的 `localStorage` 持久化。删除图形、关闭详情页、刷新页面或重启浏览器后，再次创建同类型图形仍使用最近一次颜色及透明度；不得因为 24 色升级为 80 色而清空已有 v1 偏好。
- 点击色块、自定义颜色入口和透明度滑块属于颜色选择器内部交互，不得触发画布空白点击，也不得意外关闭图形悬浮工具栏；点击选择器外部时正常收起颜色面板。

#### 实现与验收标准

- 将 80 色常量从 `MarketView.vue` 的页面逻辑中抽离为可复用、只读的画线调色板模块；`DrawingColorPicker.vue` 只负责渲染、颜色解析、Alpha 合成和事件，不在模板中重复硬编码色值。
- 更新 Playwright 中当前“24 个预设色块”的断言为 80，并覆盖 8×10 顺序、预设色保留 Alpha、自定义 RGB、0%/20%/100% Alpha、滑块圆点颜色及百分比显示。
- 对水平线、垂直线、箱体线和文本框分别验证：修改颜色/透明度 → 删除实例 → 重新创建 → 刷新浏览器 → 再次创建，样式均保持；同时确认箱体边框色与填充色互不覆盖。
- 键盘可以聚焦和选择每个色块、打开自定义颜色入口并操作透明度；控件保留明确的中文 `aria-label`，不能只靠色觉区分当前选中项。
- 执行 `npm run build` 与相关 Playwright 用例；在浅色、深色主题下人工对照附图检查面板尺寸、色块间距、透明棋盘、滑块圆点和弹层遮挡。验证通过前状态不得进入 `DONE`。

#### 当前实现与验证证据（2026-08-30）

- 单一 `drawingPalette.ts` 已冻结 10×8、80 色常量；水平线、垂直线、箱体边框/填充、文本与笔刷均复用 `DrawingColorPicker`。色块提供非颜色依赖的激活边框、`aria-pressed` 和显式焦点环，自定义色与不透明度控件都有中文可访问名称。
- 不透明度轨道采用“当前 RGB 渐变 + 透明棋盘”双背景，滑块圆点保留高对比边框/阴影，Alpha 为 0 时仍可定位；预设色更新 RGB 时继续保留当前 Alpha。
- 已执行 `npm run build`，以及 `npx playwright test e2e/terminal.spec.ts --grep "brush drawing saves" --reporter=line`（1/1 通过），确认 80 色弹层可用、色块能由键盘 `Enter` 选择，并依次保存/显示 0%/20%/100% 不透明度而不破坏笔刷样式。历史的矩形偏好用例已从 24 色和已移除色值改为当前 80 色契约；它现在覆盖 `Escape` 关闭浮层、线条/填充 RGB 与 Alpha、删除重建和页面刷新后的独立偏好持久化。该同一组浏览器回归另覆盖水平线、垂直线和文本框各自设置不同 80 色默认值后删除、刷新、重建仍互不串扰（3/3 通过）。2026-08-30 的本机浏览器验收中，主题菜单可见“跟随系统/浅色/深色”；选择深色后根主题为 `dark`、页面背景为 `rgb(11, 14, 20)`，再选择浅色后恢复 `light` 和 `rgb(245, 247, 250)`。仍缺少真实触摸设备和所有图形的色板视觉人工验收，因此维持 `VERIFYING`。

### R4-T003 — K 线主图笔刷画线工具

- `type`：网页端画线交互；`priority`：P1；`state`：VERIFYING；`failure_count`：0。
- `目标`：在标的全屏详情页画线工具栏新增“笔刷”，用户按下后可在 K 线主图连续拖动绘制任意视觉长度的自由折线；完成后与水平线、垂直线一样可选中并显示悬浮工具栏。
- `依赖`：笔刷复用 R4-T002 抽离后的画线颜色选择器；若先实现笔刷，可先接入当前颜色组件，但不得在笔刷中再造一份色板。

#### 图形模型与兼容边界

- 将 `DrawingTool` 与画线实例类型新增 `brush`；一条笔刷保存 `points: [{time, price}, ...]`，至少 2 个点，点按手势路径顺序保留。
- 点使用时间/价格逻辑坐标而不是屏幕像素，确保缩放、窗口改变、历史数据增补和页面刷新后路径仍贴合原行情坐标。
- 后端现有画线 JSON 文档无需数据迁移；新读取端必须继续接受历史四种图形，旧画线文档不增加虚构笔刷点。
- “任意长度”指视觉路径不设最大时间/价格跨度，不等于无上限保存指针事件。采样时仅记录与前一点相距至少 2 CSS 像素的点，收笔后按像素容差简化折线；单实例最多 2,048 点，超出时自适应简化而不截断末端。
- API 保存前对笔刷点数、有限价格、时间格式和单文档总大小做边界验证；非法笔刷返回可读 422，不影响文档中其他合法图形。

#### 创建、选中与编辑交互

- 选中笔刷工具后，主图内 `pointerdown` 开始预览、`pointermove` 连续追加路径、`pointerup` 完成一条实例；只点击没有形成有效路径时不创建空图形。
- 手势期间使用 pointer capture，鼠标、触摸和手写笔共用同一条事件路径；离开主图或发生 `pointercancel` 时，已有至少 2 点则安全收笔，否则取消。`Escape` 取消当前未提交预览。
- 笔刷手势只在 K 线主图价格网格内取点，不跨入成交量/持仓量副图；绘制期间禁用画布平移和缩放冲突，收笔后恢复。
- 笔刷为自由绘制，默认不对路径中每个点做 K 线高/低价吸附；全局“磁吸”偏好不因此被改写。
- 收笔后一次性创建并保存实例，拖动过程不发送每个点的 HTTP 请求；预览使用当前 `brush` 默认样式。
- 光标工具下点击折线的任意线段即选中整条笔刷，命中容差与当前线宽联动且不低于 6 CSS 像素；选中后显示与其他线条一致的悬浮工具栏。
- 未锁定的笔刷允许整体拖动，所有点使用同一时间索引偏移和价格像素偏移，不在本轮增加单点节点编辑或橡皮擦。拖动完成时一次保存。

#### 悬浮工具栏与偏好持久化

- 悬浮工具栏包含线条颜色、线条粗细、线型（实线/虚线/点线/一长一短）、锁定/解锁、跨周期和删除；笔刷没有填充色、文字或字号控件。
- 颜色和透明度复用 `DrawingColorPicker`，固定 80 色表及自定义 Alpha 规则与 R4-T002 一致；笔刷样式改动立即更新当前实例和后续新建笔刷的默认样式。
- `market-drawing-preferences-v1` 的 `styles` 新增独立 `brush` 键，不与水平线或垂直线共用默认样式。未含 `brush` 的旧 v1 偏好使用基础默认值，不删除或重置旧四类偏好。
- 锁定只禁止画布拖动/修改，仍允许选中后解锁或删除。跨周期沿用当前 `period + crossPeriod` 规则；投影到目标周期时保留原始点，仅在显示层映射到最近可见 K 线。
- 删除实例、删除全部画线、切换标的、关闭详情页、刷新页面或重启浏览器后，笔刷默认样式、跨周期和连续画线偏好皆遵循现有持久化语义。

#### 验收标准

- 组件测试覆盖采样、折线简化、2,048 点上限、坐标转换、整体拖动、锁定、跨周期投影、指针取消和无效单击。
- API 测试覆盖一个合法长笔刷与历史四种图形混合保存/读取，以及点数超限、非有限价格和非法时间的 422；验证对旧 JSON 无迁移。
- Playwright 覆盖：选择笔刷 → 在主图拖动绘制 → 悬浮工具栏出现 → 修改颜色/粗细/线型 → 整体拖动 → 锁定 → 跨周期 → 删除/重建 → 刷新后样式仍保留。
- 卡片图只读显示笔刷但不接受编辑手势；混合存在水平线、垂直线、箱体、文本和笔刷时，隐藏全部、批量读取和删除全部仍正常。
- 用一条简化前超过 10,000 个 pointermove 样本、收笔后不超过 2,048 点的确定性性能用例验证：预览无每点 HTTP 请求，收笔和再渲染不冻结页面。
- 执行 `npm run build` 与相关 Playwright 用例；在浅色/深色主题、鼠标与触摸模拟下做人工手感验收，并记录线段命中容差、连续绘制和长线性能证据。

#### 当前实现与验证证据（2026-08-30）

- 已实现逻辑坐标采样（最小 2 CSS 像素）、收笔简化与超过 2,048 点时的自适应压缩；绘制过程只在收笔时写入一次，命中容差最小为 6 CSS 像素，笔刷独立复用 `DrawingColorPicker` 与 `market-drawing-preferences-v1.styles.brush`。
- `KLineChart` 现在在有效笔刷起笔后捕获原生指针，`pointercancel` 对已有有效路径安全收笔、无效路径取消，`Escape`、工具切换和卸载时释放捕获；绘制期间忽略滚轮缩放，避免与笔刷手势竞争。
- 已执行 `npm run build`，以及 `npx playwright test e2e/terminal.spec.ts --grep "brush drawing saves|coalesces 10k" --reporter=line`（2/2 通过）。前者覆盖中断指针后的收笔保存；后者在同一笔刷手势内同步派发 10,000 次移动，验证最多 2,048 点、仅一次 PUT 保存且派发耗时低于 5 秒。另以启用触摸能力的浏览器夹具派发 `touchstart/move/end`，验证触摸拖动生成一条有效笔刷并只在收笔后保存。真实设备手感和浅/深色人工验收尚未完成，因此维持 `VERIFYING`。

## 四张期货结构图共享契约（R4-T004～R4-T007）

以下规则同时约束“品种市值分布、席位市值分布、品种持仓分布、席位持仓分布”。各图拥有独立指标、方向和固定基准元数据，不能把某张图的排序或“其他”成员集合复用于另一张图。

### 统计范围与数据质量

- 默认范围为国内商品期货有效月份合约，排除中金所金融期货、期权、主连、次连、加权连续、商品指数和已到期合约；交易所与品种使用标准键，不仅凭来源代码前缀判断。
- 品种图汇总全部有效月份合约；席位图只汇总交易所实际公布的会员排名记录。席位图的“总计”和占比均指**已公布排名覆盖总计**，页面必须展示来源、公布名次和覆盖状态，不得暗示全市场全量。
- 结构序列按交易日对齐；缺失值返回 `null` 并形成断点，不以前值或 `0` 填充。无法满足完整日门槛的日期标记 `PARTIAL`，不得作为基准交易日。
- 各值必须保留 `formulaVersion`、`priceBasis`（市值图）、输入覆盖、来源、计算时间和不可用原因；金额 API 使用原始人民币值，显示层可换算亿元。

### 固定堆叠与“其他”集合

- 图形统一为普通数值堆叠面积图，不是 100 条普通折线，也不是把每日占比归一到 100% 的百分比堆叠图；纵轴保留真实金额或持仓量，悬浮提示另外显示当日占比。
- 首次生成某个“图表+指标+方向”的元数据时，以最新完整交易日作为 `baselineDay`，按该日数值从大到小生成固定 `stackOrder`：最大成员位于底层，依次向上堆叠。后续交易日只追加数值，不因排名变化自动重排。
- 基准日占对应总计比例低于 `1.5%` 的成员组成固定 `otherMembers`；主图将它们汇总为“其他”。阈值判断使用未格式化原值，等于 `1.5%` 的成员保留为独立层。
- 点击主图“其他”或其图例，进入二级堆叠面积图，展示 `otherMembers` 中全部真实成员；二级图沿用各成员在基准日的固定降序，不再生成第二层“其他”。返回主图后保留时间范围和筛选条件。
- `stackOrder`、`primaryMembers`、`otherMembers`、颜色索引、基准日、阈值与版本属于服务端持久化业务元数据，不能只存浏览器。重新选基准或调整阈值必须发布显式新版本并记录原因，不得由每日更新任务自动重算。
- 基准发布后首次出现的新成员进入显式 `unclassifiedMembers` 质量状态；在新基准版本发布前不得静默插入原顺序或并入固定“其他”，页面必须显示“存在未纳入当前基准的新成员”及其对总计的影响。

### 颜色、交互与接口

- 复用 ECharts 6。使用 12～24 个可循环颜色的感知差异色板，按固定堆叠顺序分配并持久化颜色索引；同一图内相邻层必须有可辨边界，不能只用同一色相的轻微明暗变化。
- 悬浮某层或图例时高亮目标层，其余层透明度降至 `0.10～0.25`；移出后恢复。键盘聚焦图例必须产生等价状态，颜色不是唯一识别手段。
- 顶部提供近 1 年、3 年、5 年、全部四个范围；默认近 1 年。切换范围不得改变固定顺序、颜色或“其他”集合。
- 接口只返回绘图和追溯必需字段：`chartId`、`metric`、`direction`、`unit`、`baselineDay`、`baselineVersion`、`threshold`、`stackOrder`、`primaryMembers`、`otherMembers`、`unclassifiedMembers`、`dates`、`series`、`totals`、`unclassifiedTotals`、`coverage`、`formulaVersion`、`priceBasis`、`source`、`updatedAt`。`level=other` 只返回固定“其他”成员的明细系列。
- 目标数据量为约 100 个品种或 500 个已公布席位、最长全部历史日线。API 不返回未使用大字段；前端按需加载二级图，并通过关闭动画、渐进渲染、降采样验证和 ECharts 实例复用控制内存与交互延迟，但不得为性能静默丢成员。

### 一致性验收

- 每个交易日验证：主图独立层之和 + “其他” + `unclassifiedTotals` = 该指标可计算总计；二级图各成员之和 = 主图“其他”。允许显示格式四舍五入误差，不允许底层数值误差。
- 用一组会发生排名交叉的确定性样本证明堆叠顺序不随日期变化；用阈值上下边界样本证明“其他”成员不漂移；用新上市成员样本证明不会静默改变基准元数据。
- Playwright 覆盖四个时间范围、图层/图例悬浮联动、目标高亮与其他层淡化、“其他”下钻与返回、浅色/深色主题、键盘访问、空状态、部分覆盖和 API 失败降级。

### R4-T004 — 中国商品期货品种市值结构堆叠面积图

- `alias`：`R4-FUTURES-MARKET-CAP-STACKED-AREA`；`type`：期货数据工程与网页图表；`priority`：P1；`state`：VERIFYING；`failure_count`：0。
- `目标`：在“品种市值分布”模块展示所有商品期货品种名义持仓规模的固定顺序堆叠面积图，让用户查看市场规模、结构占比和长期变化。
- `依赖`：`R4-T001` 页面和筛选骨架、有效月份合约目录、按交易日生效的合约乘数、`FUTURES_CONTRACT_DAILY_SNAPSHOT`、本共享契约。
- `实现证据（2026-09-01）`：用户已确认 `priceBasis=SETTLEMENT`。新增离线 `futures-notional-structure`，它仅从质量通过的商品期货有效月份合约日线读取结算价和单边持仓量，并从同交易日 `FUTURES_CONTRACT_RULE_SNAPSHOT` 解析交易乘数；缺任一输入不写数值、不借用邻日规则。真实本机构建扫描 5,987 个 Silver 文件、准备 104,116 个合约日聚合，写入 17,553 条 `product-notional` Gold 成员记录（2023-09-05～2026-08-21），无拒绝行；固定基准日为 2026-08-21。API 和页面都返回 `priceBasis=SETTLEMENT`、公式版本 `futures-structure-notional-settlement-v1` 以及固定“其他”下钻。定向 Python、Ruff 和 Vue 构建已通过；尚待浏览器交互和性能验收。

#### 指标公式与聚合

- 合约名义持仓规模：`contractNotional = settlement × contractMultiplier × openInterest`。`contractMultiplier = tradingUnit ÷ quoteUnit`；`openInterest` 使用交易所公布的单边持仓量，不额外乘 `2`，也不乘保证金率。
- 品种名义持仓规模：同一 `exchange + product + tradingDay` 下全部可计算有效月份合约 `contractNotional` 之和；市场总规模为全部纳入品种之和。
- 同一 `formulaVersion` 必须统一 `priceBasis=SETTLEMENT`。缺结算价、乘数或持仓量的合约不参与数值，并计入缺失覆盖，不能用收盘价或另一交易日规则逐行回退。
- 页面标题可保留“品种市值分布”，副标题、纵轴说明、图例帮助和导出字段必须写“名义持仓规模”，不得写成沉淀资金、成交额或客户保证金。

#### 专项交互与验收

- 主图悬浮提示至少显示品种名称、品种代码、交易所、交易日、名义持仓规模、当日市场总规模、占比、`priceBasis` 和覆盖状态；“其他”提示同时显示固定成员数。
- 外部同口径资料中的约 `43,027.94 亿元` 仅作为非硬编码合理区间参考，不是测试目标；若差异明显，应从合约范围、价格源、乘数版本、单位、单/双边口径和交易日解释，禁止为对齐数值添加常数或补丁。
- 单元/API 测试覆盖跨月份求和、跨交易所同代码隔离、单边不乘二、与沉淀资金分离、价格基准一致、缺字段覆盖和市场合计恒等式；记录至少一个完整真实交易日的可复现核对表。

### R4-T005 — 中国商品期货席位市值结构堆叠面积图

- `type`：期货席位数据工程与网页图表；`priority`：P1；`state`：BLOCKED；`failure_count`：0。
- `目标`：在“席位市值分布”模块展示交易所已公布排名覆盖范围内各会员席位的名义持仓规模结构，并明确其不是会员全部客户资金或全市场全量。
- `依赖`：`R4-T004` 已锁定的结算价公式版本、`FUTURES_MEMBER_POSITION_DAILY` 的真实全合约采集、席位规范名和来源覆盖探针；固定 `RB2610 / 2026-08-07` 样例不能作为验收数据。
- `阻塞证据（2026-08-29～2026-09-01）`：固定 `RB2610 / 2026-08-07` 样例已移除，采集器改为按交易日逐所拉取并持久化 `FUTURES_MEMBER_POSITION_DAILY` 的原始方向排名；真实 `2026-08-28` 采集获得 CFFEX 880、CZCE 4,856、SHFE/INE 3,272、GFEX 480 条排名，但 DCE 端点返回 `BadZipFile: File is not a zip file`。2026-08-30 对同一大商所官方 `batchDownload` 入口以 HTTPS、官方 Referer 和浏览器 User-Agent 做只读直连探针，仍无响应至请求超时，不能把无效 HTML/超时响应作为 ZIP 解析或改用未审计的第三方镜像。`priceBasis=SETTLEMENT` 已于 2026-09-01 锁定；当前唯一阻塞是商品交易所席位来源覆盖不完整，不能生产或展示伪全市场席位市值序列。

#### 指标公式与方向

- 席位多头/空头名义持仓规模分别为 `directionPosition × settlement × contractMultiplier`，方向持仓已是单边明细，不乘 `2`；跨品种、合约求和时保留交易所边界。
- 默认 `direction=gross`，值为多头规模 + 空头规模；同时支持 `long`、`short`、`netLong=max(long-short, 0)` 和 `netShort=max(short-long, 0)`。每个方向拥有独立基准、顺序和“其他”集合，净方向不得以有正有负的单一普通堆叠实现。
- 席位键为 `exchange + normalizedMemberName`；没有经审计的别名关系时，不跨交易所合并同名席位。原始席位名、规范名、公布名次和来源都必须可追溯。

#### 专项交互与验收

- 悬浮提示至少显示席位原名/规范名、交易所、方向、交易日、名义持仓规模、已公布席位总规模、覆盖名次、占比、来源和 `priceBasis`。
- 单元/API 测试覆盖同席位跨合约汇总、不同交易所同名隔离、各方向不乘二、毛值与净方向、缺失排名、同一交易日来源去重和已公布覆盖合计；页面显著显示覆盖说明。

### R4-T006 — 中国商品期货品种持仓结构堆叠面积图

- `type`：期货数据工程与网页图表；`priority`：P1；`state`：VERIFYING；`failure_count`：0。
- `目标`：在“品种持仓分布”模块展示所有商品期货品种单边持仓量的固定顺序堆叠面积图，用真实张数观察品种结构变化，不把连续合约持仓重复计入。
- `依赖`：`R4-T001`、有效月份合约目录、`FUTURES_CONTRACT_DAILY_SNAPSHOT` 和本共享契约。
- `实现证据（2026-08-29、2026-08-30）`：已登记 `FUTURES_STRUCTURE_DAILY / FUTURES_STRUCTURE_BASELINE`，离线 `futures-structure` 先由 DuckDB 在 Silver 聚合至“交易日×具体合约”再筛选有效合约，仅允许 `SHFE / INE / DCE / CZCE / GFEX`。本机全量构建写入 18,080 条成员日度记录（2023-09-05～2026-08-27），固定基准为 2026-08-27、22 个主图层；`TDX.*` 伪交易所键经范围审计已排除。API 与页面已实现范围切换和“其他”下钻；定向 Playwright 另以 100 个成员 × 500 个交易日的 50,000 点合成结构验证图表可渲染、缩放后仍可见且没有通过 API 截断成员。仍待完整浏览器/性能回归后才可标记 `DONE`。

#### 指标公式与验收

- 品种持仓量：同一 `exchange + product + tradingDay` 下全部有效月份合约交易所单边 `openInterest` 之和；市场持仓总量为所有纳入品种之和，单位为张。
- 不使用价格、合约乘数、保证金率或双边系数；不得把主连、加权或指数序列持仓再加一次。缺持仓量保持不可用并计入覆盖。
- 悬浮提示至少显示品种名称、代码、交易所、交易日、品种持仓量、当日市场总持仓量、占比和覆盖状态。
- 单元/API 测试覆盖多月份求和、连续/加权排除、到期合约排除、跨交易所隔离、缺值覆盖和总持仓恒等式；与 `R4-T004` 使用不同的固定排序和“其他”集合。

### R4-T007 — 中国商品期货席位持仓结构堆叠面积图

- `type`：期货席位数据工程与网页图表；`priority`：P1；`state`：BLOCKED；`failure_count`：0。
- `目标`：在“席位持仓分布”模块展示交易所已公布排名覆盖范围内各会员席位的持仓量结构，支持方向切换并保留公布覆盖边界。
- `依赖`：`FUTURES_MEMBER_POSITION_DAILY` 的真实全合约采集、席位规范名、来源覆盖探针和本共享契约；真实多交易所覆盖未通过前保持 `ANALYSIS`。
- `实现与阻塞证据（2026-08-29、2026-08-30）`：已建立 `FUTURES_MEMBER_POSITION_DAILY`、按多/空方向保留原始席位名、规范键、名次、增减、来源与交易所；`futures-member-structure` 分方向生成 `long / short / gross / net-long / net-short` 的 `FUTURES_STRUCTURE_DAILY`，净方向只在同一交易所、合约、席位两侧均已公布时计算，其余保持缺失。2026-08-30 起另持久化每个交易日/交易所/来源的排名采集覆盖（状态、合约数、排名行数、错误与获取时间）；`/api/futures/member-positions` 不再仅凭现有排名行推断覆盖，旧排名会明确标为 `LEGACY_UNVERIFIED`，失败的最新采集也会作为无排名但有日期的 `FAILED` 结果展示。为避免启动全市场抓取，受控 `fetch --dataset FUTURES_OI_LEADERBOARD` 在 2026-08-28 真实写入 9,488 条方向排名：CFFEX 880、CZCE 4,856、SHFE 2,951、INE 321、GFEX 480，DCE 为 `BadZipFile` 失败；商品范围为 8,608 条并在 API 中明确仅缺 DCE。该日 `futures-member-structure` 写入五个方向的部分覆盖成员行（391 / 326 / 194 / 194 / 336），但五个方向均拒绝建立正式基准。席位结构只有五个商品交易所均有显式 `PASS` 覆盖时才建立固定基准，避免以部分成功行生成完整堆叠图；网页已提供方向、范围、其他下钻、具体月份合约席位表及逐交易所来源覆盖。新 `batchDownload` 官方入口的直连请求超时；同日以 `2026-08-28` 与普通浏览器 User-Agent 对旧的官方逐合约 `memberDealPosiQuotes.html` 做单请求探针，返回 HTTP 412 防自动化 HTML，不是可解析排名表。两者均不能作为可用回退，未接入任何未审计镜像。不能以此生产完整席位持仓堆叠图或伪造全市场覆盖。

#### 指标公式与方向

- 默认 `direction=gross`，值为席位多头持仓 + 空头持仓；同时支持 `long`、`short`、`netLong=max(long-short, 0)` 和 `netShort=max(short-long, 0)`，单位均为张。
- 各方向按 `exchange + normalizedMemberName + tradingDay` 汇总已公布的商品期货品种和合约记录；每个方向独立生成基准、顺序和“其他”集合。
- 净方向只展示对应方向的正值，避免负面积破坏普通堆叠语义；完整的有符号净持仓仍由“商品合约席位分布”明细模块提供。

#### 专项交互与验收

- 悬浮提示至少显示席位原名/规范名、交易所、方向、交易日、持仓量、已公布席位总持仓量、占比、覆盖名次和来源。
- 单元/API 测试覆盖跨品种/合约汇总、同名席位隔离、毛持仓、两个净方向、重复来源记录、排名缺口、部分交易所失败和已公布覆盖合计；Playwright 验证切换方向时基准版本、顺序、颜色和“其他”集合随方向正确切换。

### R4-T008 — 中国商品期货多空热度指标、仪表盘与历史趋势图

- `alias`：`R4-FUTURES-LONG-SHORT-HEAT`；`type`：期货 Gold 数据、FastAPI 与 Vue 图表；`priority`：P1；`state`：DONE；`failure_count`：0。
- `目标`：在独立 `/futures/` 页面提供总多空、品种多空、资金多空三类 `[-100,+100]` 热度，以 3 个半圆 Gauge 展示最新状态，并在一张历史折线图中展示三类热度的每日变化。
- `范围边界`：仅使用本地标准行情和可追溯合约规格，不调用参考站接口，不复制或声称复现其未公开算法；本轮不新增第四个背离 Gauge，也不把状态文字写成买卖建议。

#### 统计范围与品种方向

- 动态读取 SHFE、INE、DCE、CZCE、GFEX 商品期货品种，默认排除 CFFEX；不在前端硬编码品种代码。
- 品种方向优先使用项目标准加权品种的相邻交易日结算收益；若当日没有合格加权品种序列，则从有效月份合约回算：`r(i,j,t)=ln(settlement(t)/settlement(t-1))`，以当日 `settlement × multiplier × openInterest` 占品种合计的比例加权。合约乘数在同品种同日相同而约去时仍需在方法中声明，不能把缺失乘数伪装为已验证资金字段。
- `R(i,t)>neutralThreshold` 为上涨，`R(i,t)<-neutralThreshold` 为下跌，其余为平盘；第一版 `neutralThreshold=0`。无前结算、非正价格、无持仓量或无有效成分的品种从方向统计中排除并计入覆盖。
- 现有 `FUTURES_BREADTH` 继续代表旧合约/主力系列涨跌家数；本任务新增品种方向，不修改旧表及旧接口含义。

#### 三类热度公式

- 日度品种分数：`BreadthScore=(upVarietyCount-downVarietyCount)/(upVarietyCount+downVarietyCount)×100`；平盘不进入分母，无有效方向品种时返回 `null`。
- 品种沉淀资金复用 `futures.py` 的版本化公式 `settlement × contractMultiplier × openInterest × marginRate × sideCount`；国内单边 OI 的现有审计结论使用 `sideCount=2`。缺少任一输入的合约不补零，按品种和日期记录资金覆盖。
- 日度资金分数：`FundScore=(upFund-downFund)/(upFund+downFund)×100`；平盘资金仅作说明，不进入分母。资金品种覆盖率低于集中配置的 `80%` 或分母为零时，`FundScore` 返回 `null`，总热度同时不可用。
- 最近 10 个有效交易日使用指数衰减：`rawWeight(k)=2^(-k/3)`，按实际窗口归一化后分别计算 `BreadthScore10` 与 `FundScore10`。不足 10 日不补零，使用已有日重新归一化并标记 `isWarmup=true`；任意历史点只能读取当日及此前数据。
- 总热度不作为固定 Gold 真值保存：`TotalScore10(user)=breadthWeight×BreadthScore10+fundWeight×FundScore10`。默认品种 40%、资金 60%，两者之和恒为 1；底层任一值不可用时总热度不可用。
- 背离为 `Divergence=BreadthScore10-FundScore10`；绝对值小于 10 为基本一致，大于等于 10 为品种强于资金，小于等于 -10 为资金强于品种。本轮只在 Gauge 附近或 Tooltip 展示文字。

#### 集中配置、Gold 与 API

- 核心参数集中在包内只读配置 `futures_long_short_heat.json`：公式版本、10 日窗口、半衰期、默认用户权重、滑块步长、分数范围、中性阈值、背离阈值、资金最低覆盖、排除交易所和状态区间；Python、SQL 和 Vue 不重复硬编码另一版本。
- 新增 `FUTURES_LONG_SHORT_HEAT` / `futures_long_short_heat_daily`，主键为 `formulaVersion + tradingDate`，至少保存品种总数、方向/资金有效与缺失数、双覆盖率、上涨/下跌/平盘品种和沉淀资金、两个日度分数、两个 10 日分数、背离、预热、质量状态、来源截止日、计算方法和时间。不同公式版本并存，同版本同日期幂等更新。
- 新增受控离线构建命令，从 CN FUTURE 日线 Silver 读取标准加权与月份合约并写 Gold；API 请求只读 Gold，滑块交互不触发 Silver 扫描。
- `GET /api/futures/heat?start_day=&end_day=` 返回集中配置和基础历史点；缺表或无数据返回 `200 + available=false + points=[]`，非法日期返回 422。金额使用未格式化人民币原值，缺失值为 `null`，不得返回 NaN/Infinity。

#### Gauge、滑块与历史图

- 顶部横向展示“总多空热度、品种多空热度、资金多空热度”三个半圆 Gauge，统一 `-100～+100`。负值使用绿色偏空体系、正值使用红色偏多体系；数字保留正负号和状态文字，Canvas 之外同时保留可访问的真实文本。
- 状态区间由集中配置决定，默认依次为极度偏空、明显偏空、偏空、多空均衡、偏多、明显偏多、极度偏多；状态只描述市场，不出现买入/卖出文案。
- 总 Gauge 下方使用单控制点滑块调整资金权重，品种权重自动等于 `1-fundWeight`；默认 40/60、步长 5%，拖动时当前总 Gauge 与可见历史总线实时轻量更新，无需“应用”。
- 当前没有账户级通用配置服务，使用 `localStorage` 键 `marketlistener.futures.longShortHeat.fundWeight`；读取时验证有限范围，刷新/重进恢复，点击“恢复默认”后写回集中配置默认值。存储不可用时仅降级为当前会话。
- Gauge 下方一张多序列折线图同时显示总热度、品种热度和资金热度；Y 轴固定 `[-100,+100]`，显示弱化的 0 中性线，三条指标身份线使用 Design Token 中固定且深浅主题可辨的颜色，不随正负改变颜色。
- 支持 1 个月、3 个月、6 个月、1 年、3 年、5 年、全部范围；只改变显示窗口，不改变 10 日算法。Tooltip 同一交易日展示三值、当前权重、品种数量、上涨/下跌沉淀资金、覆盖和预热状态；图例默认三线全开并可显示/隐藏。
- Gauge 三值全部取当前筛选基础序列最后一个共同交易日；历史最后一点与对应 Gauge 必须严格相等。滑块只线性重算总线，不改品种线/资金线，不重新请求或扫描行情。

#### 实际实现与真实数据证据（2026-08-27）

- 新增 `futures_long_short_heat_daily`，以 `(formula_version, trade_date)` 为复合主键；旧单日期主键可事务迁移，不同公式版本并存。当前公式 `2026-08-v1` 已按统一日历重建 `2005-01-19～2026-08-21` 共 `5,244` 个交易日，历史固定总热度未入库。
- `futures-calendar-sync` 持久化 `8,797` 个统一交易日；Gold 重建据此排除了 Silver 中 `2006-01-26/27` 两个非交易日的 10 条遗留记录，并以原子区间替换清除旧 Gold 点。
- `futures-rule-sync` 已回补 `2021-04-14～2026-08-21` 的 `1,285` 个真实交易日，共保存 `82,141` 条品种规则和 `91,633` 条具体合约保证金 override；规则包含逐日 Provider 与检索时间，计算只接受同日快照，缺日不向前猜测。公共上游对 15 个日期稳定返回空表，同步器会保留其余成功日并把失败日显式返回，重跑只请求缺日。
- 最新日共有 74 个非中金所品种：方向有效 74、资金有效 73；`BreadthScore10=33.2265866769`、`FundScore10=42.8198412950`、`Divergence=-9.5932546180`、`isWarmup=false`。默认 40/60 的用户总热度在浏览器动态得到约 `+38.98`。
- `/api/futures/heat` 只读当前公式 Gold，金额返回人民币原值，非法日期为 422；Vue `/futures/` 完成三 Gauge、七档状态、绿空红多、单滑块、本地持久化、七档时间范围、固定三线颜色、0 轴、同日 Tooltip 和响应式布局。
- 真实规则表由 `AKShare futures_rule` 所封装的国泰君安期货交易规则表提供，统一日历由 `AKShare tool_trade_date_hist_sina` 提供；二者均已本地快照化但仍属于第三方公共来源。剩余 1 个资金字段缺口和来源替换风险作为已知限制保留。
- 长历史资金线受本地月份合约 Silver 覆盖约束：`FundScore10` 当前可用 `237/5,244` 日；近 1 年为 `222/243=91.36%`，近 5 年为 `237/1,211=19.57%`。API 对任意所选区间返回资金/总热度历史覆盖和最早可用日，缺失日保持 `null`；不能据此声称已具备完整 5 年资金历史。
- 最终验收通过：当前桌面套件共收集并通过 `771` 项测试，Vue 生产构建通过，完整 Playwright `25/25` 通过，新增逐值一致性/重进持久化断言的多空热度专项再次 `6/6` 通过；`scripts/verify.ps1` 的 Ruff、共享 Schema、桌面 pytest、Android lint/JVM 单测和 Debug APK 构建全部通过。真实 Silver→Gold 最终重跑仍写入 `5,244` 日且最新覆盖率不变。

#### 验收标准

- Python 测试覆盖指数权重严格递减、和为 1、`T-3/T=0.5`，70/30 得 +40，300/100 得 +50，两组总权重得 +50/+35，CFFEX 排除、平盘分母、稀疏交易日、预热重归一化、无未来数据、缺字段、资金覆盖门控和公式版本幂等。
- API/存储测试覆盖空库、日期过滤、可空资金字段、不同公式版本并存、无固定总热度真值、最新点复用、非法日期和 JSON 有限数；真实构建报告分别记录方向覆盖与资金覆盖。
- Playwright 覆盖三 Gauge、40/60 与 70/30 算例、总历史线全窗口联动、基础两线不变、刷新/重进持久化、恢复默认、图例/Tooltip/0 轴、深浅主题、预热/低覆盖/空态，以及桌面三列、窄屏 2+1/单列布局。
- 性能样本至少包含 5,000 个交易日点；拖动滑块不新增网络请求，不扫描 Silver，Gauge 和总线更新无明显卡顿。执行 Ruff、定向与全量 pytest、Vue build、定向与全量 Playwright；真实资金覆盖达到门槛前任务不得标记 `DONE`。

### R4-T009 — 网页端“数据”页面三分区与中港市场/宏观监测

- `alias`：`R4-DATA-PAGE-MARKET-MACRO-SECTIONS`；`type`：市场数据工程、宏观数据与网页信息架构；`priority`：P1；`state`：BLOCKED；`failure_count`：1。
- `目标`：将现有 `/data` 页面重构为且仅为三个一级业务分区：A 股、港股、其他数据。A 股和港股分区提供可追溯的市场总览、广度和状态名单；其他数据按国家/地区组织宏观序列，避免把中国、美国及不同频率、单位的指标混在一个无层级面板中。
- `当前基线`：`DataView.vue` 目前以“我的仪表盘、动态排行、热力图、数据浏览器”和通用面板分类为主，尚无三分区业务结构。后端已有 `A_SHARE_BREADTH`、`CN_MARGIN`、`HSGT_FLOW`、`CN_ZT_POOL` 和 `MACRO_SERIES` 的部分采集，但 A 股尚未按四类板块拆分，涨跌停未覆盖一字板与炸板，融资衍生项口径不完整，港股总览缺失，宏观登记项也只覆盖本任务清单的一部分。
- `范围边界`：本任务只重构网页端“数据”页面及其市场/宏观数据链路，不替代独立 `/futures/` 页面、行情页或数据源管理页。现有个人仪表盘和数据浏览器不得丢失，迁移为页面顶部“数据工具”入口或侧栏抽屉，不得成为第四个一级业务分区。R3-T008 的通达信证券单位问题继续有效；R3-T009 的 TickDB 本地原始目录已由用户删除且从未形成 Silver，不能用已知错误分区、历史下载记录或不存在的原始数据冒充正式面板数据。
- `实现证据（2026-08-29、2026-08-30）`：`/api/data/sections` 已公开且仅公开 A 股、港股、其他数据三项一级业务元数据；`/api/data/macro/catalog` 和 `macro/series` 以本地 `gold_metrics` 为唯一观测来源，支持中国/美国目录、时间线和 M2 季节视图所需的 1～12 月断点数据。页面已在 M2 下提供“时间序列/季节图”切换，季节图每年一条线且保留缺月 `null`。2026-08-30 的后续单任务 AkShare 宏观采集成功写入 20,857 条 Gold 观测、无错误：进口、出口、贸易差额、社零同比/环比、外储和季调后非农均有已登记的标准化链路；页面当前可用中国 14/15、美国 2/4 条登记序列。外贸、外储与非农上游只给出来源公布日期，目录以 `timeBasis=SOURCE_DATE` 和“来源日期”显示，不能伪称统计观察期；M0 最新观测为 2026-07 的 11.6%。已修复数据页对过期 IndexedDB 观测先渲染、后台刷新却不回填 UI 的问题：R4 市场/宏观面板强制读取当前本机 API，浏览器实际重载后立即显示本轮新增序列。社会用电量原始单位、美国进口金额、美国核心 PCE 年化季率终值仍没有同口径的已验证来源，继续 `UNAVAILABLE`，不以相近指标填充。API 将权威发布日期保持为 `releasedAt=null`，本机取得时间单列为 `fetchedAt`，不再混淆两者。`/api/data/equities/cn/overview` 将本机 Gold 中混用的 `YYYY-MM-DD`/`YYYYMMDD` 交易日归一后返回可追溯历史；当前两日 2026-08-07、2026-08-21 均含总市值、成交额和涨跌家数，最新日另含权威涨/跌停池。A 股卡片和走势只直读 `A_SHARE_BREADTH / CN_ZT_POOL`：四板块拆分、一字板、炸板率保持 `PARTIAL`/不可用；本机 HSGT 的“北向”键未同时含当日净买额、累计净买额和持股市值，且完整三字段落在“南向”键，故北向面板继续 `UNAVAILABLE`，不改名复用。港股总览在切换至港股分区后按需汇总本机 `tdx-cn-v2` 港股股票日线：真实最新日 2026-08-18 覆盖 2,706 个标的、成交额 1,943.23 亿港元、上涨/持平/下跌 1,059/574/1,073；返回每个交易日的覆盖数，明确为 `PARTIAL`，不套用 A 股数据或当前 F10 市值。`/api/data/equities/{market}/lists` 已建立带日期、分页和类型校验的状态名单契约；本地没有带生效期/公告的权威记录时返回 `available=false + items=[] + limitation`，不让空表伪装成“没有风险标的”。页面同时列出所有 R4 A/H 面板及各自不可用原因。未采集或未通过来源门的数据保持 `UNAVAILABLE`，不会生成演示值；原个人仪表盘、排行、热图和数据浏览器仍保留在本页“数据工具”区域。港股市值/状态名单及尚无真实采集的宏观系列仍未达到本任务完成门槛。
- 2026-08-30 后续仅运行 `MACRO_SERIES`：新增 241 条 `CN_ELECTRICITY_CONSUMPTION` Gold 观测，使中国目录为 15/15 可用。该序列注册为“全社会用电量（年内累计）”，原始表值以万千瓦时计并 `/10000` 后入库为亿千瓦时；最新 `2026-07 = 61,399`，与[国家能源局 2026-08-21 公告](https://www.nea.gov.cn/20260821/f227e3fb9f05422285e354f4c27aec07/c.html)的 1～7 月累计值交叉一致。它不得被解释为单月用电量。
- 2026-08-30 继续通过 BEA 官网当前发布页发现并读取[美国商品和服务贸易历史工作簿](https://bea.gov/data/intl-trade-investment/international-trade-goods-and-services)：解析器只接受 Table 1 的 “Imports / Total” 季调、百万美元列，明确不读取相邻的 Goods 或 Services 列。真实来源返回 414 条 `US_IMPORTS_SA`，范围 `1992-01`～`2026-06`、最新 `387,989` 百万美元。
- BEA 公共 NIPA 季度原始文件的 `DPCCRG`（PCE excluding food and energy）可无凭据获得。代码以相邻季度指数计算 `((本季/上季)^4−1)×100`，并从[当前 GDP 发布页](https://bea.gov/data/gdp/gross-domestic-product)解析估计阶段：只有已进入第三次估计的季度才写入 `US_CORE_PCE_QOQ_SAAR_FINAL`。实际写入 268 条（`1959-Q2`～`2026-Q1`），最新 `4.421729965446342%`；当前 GDP 为 `2026Q2` 二次估计，故 Q2 明确排除。历史点使用 BEA 当前修订口径，不把它们伪称为当时未修订的发布版本。美国目录现为 4/4 可用。
- “其他数据”当前先由中国/美国切换确定国家，再按固定的货币与利率、物价、贸易、消费与生产、就业、外汇主题分组；服务端每个 `seriesId` 只登记一个主题，前端按该主题排序。定向数据页 Playwright 复核主题标题、M2 季节图、三分区 URL 恢复和港股总览的“真实点或明确限制”契约均通过；它不再假设本机港股按需聚合必定未生成。
- **当前阻塞（2026-08-30）**：宏观必做目录已达中国 15/15、美国 4/4，但 A/H 分区仍缺带生效日期的逐日上市/流通股本主数据、交易所状态公告/名单、四板块点时态归属、合法涨跌停规则/一字板/炸板明细、北向完整三字段与沪深京两融字段；用户也尚未锁定高振幅低涨幅阈值及其会话持久化规则。不能用当前 F10、名称猜测、全市场近似、A 股字段或模拟名单填补这些事实。因此本任务从 `CODING` 改为 `BLOCKED`；来源或阈值决策到位后从本条证据恢复，既有可用面板保持运行。
- 2026-08-30 补齐 A 股风险/状态名单的前端日期与分页交互：页面将 `asOfDay`、当前页和固定每页 50 条原样传入本地列表 API；名单类型或日期变化时从第一页重新查询，真实可用名单总数超过一页时才显示分页器。隔离 Playwright 夹具验证第 2 页重新请求且渲染第 2 页数据，避免以固定首页或浏览器内存切片伪装后端分页。Vue 生产构建通过；由于正式点时态名单来源仍未落库，本项不改变其 `UNAVAILABLE` 事实。

#### 页面信息架构与通用交互

- `/data` 默认进入“A 股”，顶部固定三个分区切换项：`A股 / 港股 / 其他数据`；当前分区写入 URL 查询参数 `section=cn|hk|other`，支持刷新恢复、浏览器前进后退和可复制链接，不把分区偏好当作业务数据保存。
- A 股和港股分区内先显示关键最新值与数据日期，再显示历史图、分类图和名单。其他数据先按国家/地区分组，再按“货币与利率、物价、贸易、消费与生产、就业”等主题排列；同一指标只出现在一个规范分组中。
- 每个图表和名单必须显示 `asOfDate/observationPeriod`、来源、单位、频率、更新时间、覆盖率和 `PASS/PARTIAL/UNAVAILABLE`；缺失返回空态与原因，不显示全零曲线、模拟名单或无来源估算。
- 图表共用 1 年、3 年、5 年、全部时间范围；月度/季度序列按观测期对齐，日度市场序列按对应市场交易日对齐。不同单位默认不共轴；确需同图时使用明确的独立 Y 轴，不做不可追溯归一化。
- 三个业务分区按需加载并复用请求缓存、并发去重和 ECharts 生命周期；切换分区不重复请求未过期数据，页面不得直接请求第三方 Provider。

#### A 股分区

1. **A 股市值与成交额**：分别展示 A 股总市值、流通市值和当日成交额历史；默认市值图显示总市值，可切换流通市值。统计范围仅为当日有效上市的人民币 A 股股票，排除 B 股、ETF、指数、债券和衍生品；金额单位为人民币，API 保存原值，页面可显示亿元/万亿元。
2. **四类板块涨跌家数**：按“沪深主板、北交所、创业板、科创板”展示上涨、持平、下跌家数及全 A 股合计。板块归属使用带生效日期的标准标的主数据；只有当日可交易且存在合法前收盘比较值的股票进入三类计数，停牌、上市首日无可比价和数据缺失分别返回，不计入“持平”。
3. **四类板块涨跌停与一字板**：分别展示涨停、跌停、一字涨停和一字跌停家数。必须按交易日应用板块、ST/风险警示、上市初期和临时无涨跌幅限制规则，优先使用交易所或经真实探针验证的权威涨跌停池；不得用统一 `±9.9%` 阈值估算。一字板定义为全天有效 OHLC 均等于当日合法涨/跌停价。
4. **高振幅且低涨幅家数**：建立集中配置的 `amplitudeThreshold` 和 `absoluteReturnThreshold`，候选定义为 `amplitude >= amplitudeThreshold AND abs(pctChange) <= absoluteReturnThreshold`；默认阈值须在真实分布审计和用户确认后发布，前端不得自行硬编码。无前收盘、停牌或 OHLC 不完整的股票不进入分母。
5. **风险与状态名单**：分别提供 ST 风险警示、退市风险警示、监管期、停牌期的个股名单，其中监管期和停牌期同时允许 ETF。名单记录标准标的、名称、证券类型、所属板块、状态、开始/预计结束或恢复日期、原因、来源和抓取时间；ST/退市状态以带生效期的交易所/证券主数据为准，不能只凭当前名称包含 `ST` 推断历史，也不能把名单称为模型预测。
6. **北向资金**：展示当前公开口径可获得的北向当日值和历史值，并明确区分沪股通、深股通、当日成交净买额、历史累计净买额和持股市值；每个字段单独探针和登记。上游不再公开或不稳定的字段返回不可用，不以成交额差、持仓变化或旧口径静默替代。
7. **沪深京融资融券**：按沪市、深市、京市分别展示融资余额、融券余额金额、融资买入额、融资净买入额、融券净卖出量和两融余额占流通市值比。`融资净买入额 = 融资买入额 - 融资偿还额`；`融券净卖出量 = 融券卖出量 - 融券偿还量`，单位为股/份，不能误标为金额；占比使用同市场同日的流通市值，当前以总市值计算的近似指标不得冒充正式值。
8. **涨停炸板率**：`failedLimitUpRate = failedLimitUpCount / (closedLimitUpCount + failedLimitUpCount)`；炸板股票必须在盘中触及合法涨停价且收盘未封住，分母为当日全部触及过涨停的股票。分母为零时返回 `null`；一字涨停属于封板成功，不计为炸板。

#### 港股分区

- 首批必做三项为港股总市值/流通市值、港股成交额、港股上涨/持平/下跌家数；金额保持港元原值，未经同日汇率转换不得与 A 股人民币数值直接求和。
- 港股基础统计范围为当日有效上市的港股股票，排除窝轮、牛熊证、债券、ETF 和其他衍生品；停牌、无合法前收盘或数据缺失不算持平，并分别返回覆盖数。
- 在真实来源探针通过后，可追加主板/GEM 广度、南向资金、港股通成交占比、沽空成交及停牌名单。它们属于本任务的第二阶段候选面板；未通过来源、历史长度、单位和授权验证时保持 `UNAVAILABLE`，不因 A 股存在同名指标而推断港股可用。

#### 其他数据分区

- 中国组登记并展示：M2 同比季节图、以美元计进口同比、以美元计出口同比、以美元计贸易差额、社会消费品零售总额同比/环比、PPI 同比、官方制造业 PMI、CPI 同比、外汇储备、M1 同比、M0 同比、全社会用电量、DR007、中国 10 年期国债收益率。
- 美国组登记并展示：核心 PCE 物价指数年化季率终值、经季调商品和服务进口金额、季调后非农就业人口变动、美联储目标利率区间上限。若用户所说“美国进口”实际指进口价格指数，须在进入 `CODING` 前改为独立序列，禁止在同一 `seriesId` 下更换定义。
- M2 季节图使用月份 `1～12` 为横轴、年份为系列，当前未完结年份保留缺月断点；同时提供普通时间序列视图。其余指标默认使用标准时间轴折线图，PMI 显示 50 荣枯参考线，利率与同比指标显示 0 参考线但不据此生成交易信号。
- 每个宏观序列登记稳定 `seriesId`、国家/地区、主题、官方名称、频率、单位、季调状态、名义/实际口径、来源、观测期、发布日期、抓取时间、修订/终值状态和定义版本。上游修订同一观测期时保留最新值和修订追踪，不把发布日期当成观测期，也不把月度/季度数据前向填充成日度事实。
- 中国宏观优先探针中国人民银行、国家统计局、海关总署、国家外汇管理局、国家能源局、全国银行间同业拆借中心/中国债券信息网；美国宏观优先探针 BEA、BLS、美联储和美国 Census/BEA。SDK 只可作为这些来源的适配层，必须保留最终来源与抓取证据。

#### 数据集与接口计划

- 复用并版本化现有 `A_SHARE_BREADTH`、`CN_MARGIN`、`HSGT_FLOW`、`CN_ZT_POOL` 和 `MACRO_SERIES`，不静默改变旧 `metricId` 含义；新增 `CN_EQUITY_OVERVIEW_DAILY`、`CN_SECURITY_STATUS_DAILY`、`HK_EQUITY_OVERVIEW_DAILY`，并为涨跌停规则、证券状态和宏观序列建立带生效日期/定义版本的元数据。若旧指标口径错误，发布新版本并显式停用旧版本，不原地改写历史。
- 市场总览主键至少包含 `market/segment + tradingDay + formulaVersion`；状态名单主键包含 `listType + instrumentId + effectiveFrom + source`；宏观观测主键包含 `seriesId + observationPeriod + vintage/releasedAt`。所有派生值保留输入覆盖、公式、来源和计算时间。
- `GET /api/data/sections`：返回三个一级分区、面板定义、可用状态和最后更新时间，供页面按需装载。
- `GET /api/data/equities/{market}/overview?startDay=&endDay=&segment=`：返回中港市场市值、成交额、广度、涨跌停/炸板和融资等已登记序列；仅返回该市场真实可用字段，不用空字符串代表缺失。
- `GET /api/data/equities/{market}/lists?type=&asOfDay=&segment=&page=&pageSize=`：分页返回风险警示、退市风险、监管期和停牌期名单及来源证据。
- `GET /api/data/macro/catalog?country=` 与 `GET /api/data/macro/series?seriesId=&startPeriod=&endPeriod=&view=timeline|seasonal`：分别返回国家分组目录和单序列观测；非法序列/日期返回 422，无数据返回 `200 + available=false`。
- 新接口使用 camelCase，金额和数量返回未格式化数值及单位；时间序列缺失值为 `null`，列表不以历史最后状态冒充指定 `asOfDay` 状态。现有通用 Dashboard/Data Browser 接口在迁移期间保持兼容，确认无调用方后再单独计划清理。

#### 待锁定与真实数据门槛

- 高振幅/低涨幅的两个阈值、是否允许用户在页面临时调整，以及调整值是否只影响当前会话，需要在进入 `PLAN_CREATED` 前锁定；建议先用真实全市场分布给出 `8%/2%` 等候选，而不是凭感觉直接固化。
- A/H 股历史总市值和流通市值必须验证逐日股本口径；不得用当前总股本/流通股本回乘历史收盘价。若只能取得当日快照，页面先从正式采集日起累积并明确“无历史回补”。
- 北向资金、港股扩展面板、监管期名单和美国进口序列分别执行能力探针；字段成功必须按来源、时间范围和权限逐项记录，单日样例或 SDK 中存在函数不能证明长期可用。
- 涉及个股/ETF 状态的来源必须提供可追溯原始公告或官方名单；无法获得生效区间时只能显示“当前快照”，不能伪造历史名单。

#### 推荐执行顺序与验收标准

1. 固化 A/H 股统计范围、板块归属、证券状态、涨跌停规则和宏观序列定义；完成上述待锁定项后再将状态推进到 `PLAN_CREATED`。
2. 对每个必做市场指标、名单和宏观序列执行真实来源探针，保存最早/最新日期、频率、单位、字段、修订行为、授权和 `PASS/FAILED/UNSUPPORTED`；只有通过探针的面板进入实现。
3. 先扩展数据集登记与确定性夹具，再实现增量采集、point-in-time 主数据、质量门控和 Gold 聚合；最后接入领域 API 与三分区前端，避免页面先于真实数据完成。
4. 单元/API 测试验证：四类板块互斥且合计正确，停牌不计平盘，一字板 OHLC 与合法限价一致，炸板率恒等式，融资/融券净额单位，两融占流通市值，A/H 币种隔离，名单生效区间，宏观观测期/发布日期/修订版本，以及季节图缺月断点。
5. 至少选择连续 20 个真实交易日核对 A 股四类板块和港股总览，并为每个必做宏观序列保存可复现的真实首尾值与来源；缺少历史或覆盖不足的面板保持 `PARTIAL/UNAVAILABLE`，不能用测试夹具替代真实验收。
6. Playwright 覆盖三个一级分区、URL 恢复、按需加载、市场图表、四类板块切换、名单分页与空态、国家/主题分组、M2 季节图、时间范围、深浅主题、窄屏布局、数据工具入口和局部 API 失败降级；确认页面无任何第三方网络请求。
7. 执行 Ruff、相关与全量 pytest、Vue build、相关与全量 Playwright，并同步数据集目录、接口契约、能力矩阵、架构、经验、日志和 R4 状态；必做指标未通过真实来源与口径验收前，本任务不得标记 `DONE`。

### R4-T010 — 行情待分类标的审计表与“其它”分类清退

- `alias`：`R4-MARKET-UNCLASSIFIED-REVIEW`；`type`：本地数据审计、FastAPI 与 Vue 行情页；`priority`：P0；`state`：DONE；`failure_count`：0。
- `目标`：网页端不再提供含义不明确的“其它”市场分类。凡是标准字段未命中登记规则，或通达信金融终端/期货通 `vipdoc/ds` 文件名未命中对应导入器规则的标的，统一进入一张只读“待分类标的”表，供用户确认名称、市场、交易所与资产类型；确认前不得进入正常行情分类和策略扫描范围。
- `接口与字段`：新增 `GET /api/market/unclassified?q=&page=&pageSize=&refresh=`，合并 Silver 中原“其它”标的和两个本地终端的原始未识别文件；表格至少显示标的名称、解析代码、来源代码（含数字市场前缀）、最新收盘价、最新数据时间、已有周期、来源终端和待确认原因。原始二进制文件只读最后一条完整 32 字节记录，不执行导入或改写。
- `分类约束`：内部使用非公开状态 `unclassified`，它不是市场筛选项；`/api/market/instruments` 和 `/api/strategy/matches` 均必须先排除此状态。只有把用户确认结果写入可审计的交易所/资产规则后，标的才可离开待分类表；禁止把无法判断的数据临时塞入任一正式分类。
- `真实审计证据（2026-08-28～2026-08-31）`：初始快照曾有 9,033 项，收紧金融终端浮点规则后降至 5,644 项。用户随后确认终端职责：金融终端负责证券、`10#` 基本汇率、`12#` 国际指数、`38#` 宏观、`62#` 中证、`69#` 华证和 `102#` 国证；期货通只负责国内六个期货交易所的期货序列、`42#` 商品指数及 `68#` 波动率/期权指数，期货通中重复的证券指数前缀不再展示或导入。`27#HZ`、`49#` 香港基金及 `98#02261F/H` 按用户决定退役；原始终端文件不改写。定向清理后活动 Silver 删除 `HZ` 19,044,313 根、Binance BTC/ETH 148 根、`49#` 371,520 根，以及旧规则误导入的 694 个 `CN.TDX.FUTURE` 伪期货共 1,637,374 根；`98#` 从未进入 Silver。2026-08-31 重新只读扫描待分类表为 0 项。
- `验收`：配置与网页无 `other/其它` 市场项；分类单元测试锁定 `unclassified` 非公开；原始扫描测试覆盖日线/5 分钟合并、最后收盘价、已识别文件排除和双终端来源隔离；API 测试覆盖 Silver 与 Raw 合并、分页搜索，以及待分类项不出现在正常行情；展示版本升级为 `market-categories-r4-v2`，页面回归固定“已有 v1 持久缓存时仍不显示未分类项”；Vue 生产构建通过。

### R4-T011 — 通达信证券 `tdx-cn-v2` 正式导入与来源迁移

- `type`：证券数据标准化、质量门、Silver 迁移与发布；`priority`：P0；`state`：VERIFYING；`failure_count`：0。
- `目标`：修复通达信金融终端证券 `.day/.lc5` 的资产级价格精度、成交量单位和 B 股分类，保留原始值与标准化证据；未经验证的数据不得进入权威 Silver。
- `规则`：A 股/指数日线价格除以 100，B 股/ETF/LOF/REIT 除以 1000，转债/可交债/回购除以 10000，分钟线与港股浮点价格不缩放。普通证券逐行从 `1/10/100` 倍率中选择唯一满足量额隐含均价落在 OHLC 区间的值；回购以千元面值恒等式验证；指数成交量保留 `TDX_INDEX_RAW`。无成交行继承同文件最近已验证倍率，无法唯一解释的行只进入隔离区。
- `分类与契约`：B 股使用独立 `B_SHARE`，沪 B 为 USD、深 B 为 HKD，不进入 A 股统计；Bar v1 增加可选原始 OHLCVA、价格除数、成交量倍率/单位、标准化方法/状态/版本，并补齐 LOF、REIT、转债、回购等资产枚举。
- `接口与迁移`：`import-tdx-local --audit-only` 只生成审计；`--replace-source --full-rescan` 在独立暂存库重建，备份旧 TDX 分区和目录后提升新分区，失败回滚。检查点绑定 `tdx-cn-v2`，策略升级强制重扫。数据源页读取最新审计报告，展示扫描、正式写入和隔离统计。
- `真实审计与增量（2026-08-29～2026-08-31）`：首轮可回滚来源替换提升 7,963 个 `tdx-cn-v2` 分区、200,454,925 根 PASS 行，并保留 7,478 个旧分区备份；后续浮点 `ds` 增量写入 38,663,985 根。用户确认后，金融终端新增 `10#` 基本汇率（16 标的/82,066 根）、`38#` 宏观（214 标的/470,318 根），并放宽 `12/62/69/102#` 合法派生代码；`49#` 与 `98#` 最终决定退役。期货通新增识别 261 个来源文件、写入 1,578,460 根，覆盖 `29#` 大连商品扩展序列、`47#` 中金所 L0～L3/标的指数和 `68#` 波动率/期权指数；六家国内期货交易所继续按 28/29/30/47/66 与 INE 品种拆分。Binance 采集器、数据集登记、BTC/ETH 宏观定义及活动数据全部删除。退役清理均先移到 `data_control/retired_market_data` 可恢复备份，随后只刷新受影响查询索引；未运行全量缓存重建或全套验证。
- `TickDB`：本地目录已删除且从未进入 Silver；活动下载脚本与专属测试移除，旧审计只保留为历史证据，不能作为当前数据源、回退源或质量依赖。
- `验收`：固定二进制样本覆盖股票、B 股、ETF、转债、回购、指数、量纲切换、零成交和异常隔离；完整 Ruff/pytest/Vue/Playwright/Android 验证通过；真实重建后活动 TDX 分区全部为 `tdx-cn-v2`，旧分区有可恢复备份，关键标的价格/分类/币种和远端 Git SHA 复核通过。

### R4-T012 — 行情分类维度与中文简称治理

- `alias`：`R4-MARKET-VENUE-TAXONOMY`；`type`：领域分类、通达信名称主数据、FastAPI 与 Vue 行情页；`priority`：P0；`state`：VERIFYING；`failure_count`：0。
- `目标`：公开行情分类不再把所有指数塞入“A股-指数”，也不以“全球期货/国内商品期货”掩盖真实交易场所；每个标的优先按显式交易场所分类，指数则按交易所自有系列或编制方分类。行情页删除“待分类标的”区块，内部只读审计接口继续保留供数据质量排查，未识别标的仍不得进入正式行情或策略范围。
- `指数分类`：沪深北交易所自有指数归“交易所指数”；金融终端 `62# / 69# / 102#` 分别归“中证指数 / 华证指数 / 国证指数”；`880/881` 在公开筛选中合并为“通达信指数”，内部 `series_kind` 继续保留综合/行业差异。指数编制方与挂牌交易所是不同维度，代码相同也不得跨来源自动覆盖。
- `期货分类`：国内期货按 `SHFE / INE / DCE / CZCE / CFFEX / GFEX` 六个交易场所分别展示；外盘按 `COMEX / NYMEX / CBOT` 等显式交易场所分别展示。夜盘继续作为可叠加筛选属性，商品指数和期权波动率指数保留独立指数分类；未知交易场所不得回退到“全球期货”或“商品期货”。
- `回购分类`：官方代码区间证明“债券质押式回购”只是回购业务的一个子类。当前活动库存只有沪市 `204`、深市 `1318` 通用质押式回购，公开分类只保留“债券质押式回购”；泛化“A股-债券回购”因本机为 0 项而删除。沪市 `201/202/203/205/206/207`、深市 `1319` 仍以内部 `a-other-repo` 精确识别，未来真实出现时另行决定公开展示，不得误归质押式回购或待分类。
- `中文简称`：名称优先级固定为“上传的中证/国证/华证指数清单中的官方简称 → 通达信 `ds_stk.dat`/证券名称表中的来源简称 → 已有非代码名称 → 明确带类型的代码兜底”。生成的名称表使用 `exchange + symbol` 作为键，避免同一代码在不同编制方之间串名；新导入同时读取终端名称文件，旧 Silver 行在 API 展示层用同一名称表修复。
- `验收`：分类单元测试覆盖五类指数、六家国内期货交易所、三家外盘期货交易所、夜盘兼容键和互斥回购；API 类别契约不再出现 `a-index / global-future / cn-future-commodity / tdx-board-index / tdx-industry-index` 公开项；Vue 页面不请求也不渲染待分类区块；抽样验证三份指数清单及终端缺口都能返回中文简称。按用户要求只运行相关 pytest、Ruff、Vue build 与定向 Playwright，不运行全套验证。
- `定向证据（2026-09-02）`：相关 pytest 52 项、Ruff、Vue 生产构建与行情页 Playwright 2 项通过。真实本地 API 返回交易所指数 112、中证 2,298、国证 898、华证 300、通达信指数 1,122；SHFE/INE/DCE/CZCE/CFFEX/GFEX 与 COMEX/NYMEX/CBOT 均有独立非空分类。17,706 个正式逻辑标的经展示归一后 `name == symbol` 为 0；其他债券回购库存为 0，故不暴露空筛选项。未运行全套验证，任务保持 `VERIFYING`。

## 策略系统共享架构与执行边界（R4-T013～R4-T031）

- 依赖方向固定为 `Market Data → Strategy Function → Indicator → Chart` 与 `Market Data → Strategy Function → Strategy → OrderIntent → Risk Engine → Execution Adapter → Order API`；Strategy 不直接依赖 Indicator。
- Strategy Function 是稳定 ID、显式版本、确定性、无副作用的唯一计算实现；Indicator 只负责 Plot，Strategy 只负责规则、状态、仓位、风险与订单意图。SMA、ATR 等算法不得在三层重复实现。
- 桌面 Strategy 是唯一允许产生 `OrderIntent` 的领域层，但不能绕过 Risk Engine 直接调用券商；Indicator 与 Strategy Function 的运行上下文不暴露账户、订单、持仓或执行端口。Android 继续只产生 ADR-0008 定义的观察信号。
- 第一版图表允许多个 Indicator 与最多一个 Strategy；主图指标进入价格图层，副图指标拥有独立 Pane。参数变化只重算关联资源，不刷新整页。
- 资源统一保留稳定 `id`、整数 `version`、`createdAt`、`updatedAt`、`origin=builtin|custom`、`supportedAssetTypes`、启用状态和依赖；未来 `community|plugin` 只作枚举预留，本轮不实现社区。
- 结构化 Strategy Definition 与 Rule AST 是权威可编辑表示；禁止 `eval()`、`new Function()`、浏览器任意代码、不可解析自然语言条件串和 Pine Script 兼容解释器。
- 运行状态统一显式返回 `INSUFFICIENT_DATA / UNSUPPORTED_ASSET / MISSING_FIELD / MISSING_DATASOURCE / INVALID_PARAMETER / DISABLED` 等原因；缺失值为 `null`，不补零、不造数。
- 回测、模拟、实盘三种运行模式隔离；在真实 Order API、风险引擎和执行适配器未配置并验收前，`Live` 固定禁用并显示“实盘交易接口尚未配置”。
- `定向回归（2026-09-05）`：当前工作树的 19 个策略/指标/VIX/画线后端测试文件共 190 项通过，另有 41 项共享 JSON Schema 夹具通过；相关 Python 源码与测试 Ruff 通过；Vue 生产构建通过。运行时直接加载注册表确认 39 个 Function、21 个 Indicator/绘图资源、两项内置 Strategy、三项 Template，且 `technical.atr@1/@2` 同时存在。策略页 10 个 Playwright 场景覆盖递归 AST、版本回存、报告、传输包、模板、来源边界、函数引用、筛选与自定义指标；行情联动另有 3 个场景覆盖目录/函数分层、独立 Indicator Pane（含高级指标与 VIX 溯源）及 Fibonacci 逻辑锚点，均通过。本证据不替代用户明确暂缓的全量验证和移动端验收，R4-T013～R4-T031 继续保持 `VERIFYING`。

### R4-T013 — 策略三层领域模型与共享契约

- `task_id`：`R4-T013`
- `title`：策略三层领域模型与共享契约
- `priority`：P0
- `执行对象`：Python 领域模型、JSON Schema、TypeScript 类型、领域词汇与 ADR
- `state`：VERIFYING
- `failure_count`：0
- `现状`：已有安全公式引擎、声明式 Strategy DSL、扫描 API 和简单策略页，但 Indicator、Strategy Function、Strategy 仍共享松散字典，缺少统一资源身份、版本、资产适用性、缺失状态和权限能力模型。
- `目标`：建立三层互斥资源定义、依赖图、稳定版本引用、运行状态、Plot、OrderIntent、执行模式和引用关系契约，明确桌面与 Android 的不同执行边界。
- `影响范围`：`formula_engine.py`、`strategy_dsl/`、`web_api/strategy.py`、`contracts/`、`StrategyView.vue`、`KLineChart.vue`、Android DSL 兼容边界与文档。
- `依赖关系`：ADR-0008、ADR-0009、ADR-0010；后续 R4-T014～R4-T031 全部依赖本任务。
- `技术约束`：复用现有资产类型和 canonical timeframe；契约使用 camelCase API、snake_case 持久化映射；旧 `dsl_v1/formula_v1/builder_v1` 定义必须可迁移或兼容读取。
- `验收标准`：三层依赖只能为 Function→Indicator、Function→Strategy；只有 Strategy 可产生 OrderIntent；非法跨层依赖和未知版本在保存或运行前被拒绝并返回稳定错误码。
- `测试要求`：Schema 正反例、依赖环、版本缺失、资产不兼容、权限矩阵、旧定义兼容、Python/TypeScript 字段一致性测试。
- `输出规范`：提交共享 Schema、领域类型、迁移说明、依赖关系图、错误码表、ADR/CONTEXT/ARCHITECTURE/Log 证据。

### R4-T014 — Strategy Function Registry

- `task_id`：`R4-T014`
- `title`：Strategy Function Registry
- `priority`：P0
- `执行对象`：Python 注册表、只读 API、策略函数详情 UI
- `state`：VERIFYING
- `failure_count`：0
- `现状`：`STRATEGY_FUNCTION_CATALOG` 是静态目录，ID、版本、输入输出、纯函数声明、弃用、资产适用性和被引用关系不完整。
- `目标`：建立唯一 `StrategyFunctionRegistry`，按稳定 `id@version` 注册、验证、解析与枚举策略函数，并公开输入、输出、分类、依赖及引用者。
- `影响范围`：公式引擎、DSL 解释器、策略 API、策略函数 Section、内置定义配置。
- `依赖关系`：依赖 R4-T013；被 R4-T015～R4-T021、R4-T023～R4-T028 使用。
- `技术约束`：同一 ID/版本不可覆盖；UI 改名不得改变 ID；注册表不接收账户、订单或全局可变状态；分类固定为行情数据、周期、价格、成交量、趋势、动量、波动率、统计、基本面、条件、逻辑、仓位、止损、止盈、风险、时间、工具。
- `验收标准`：`GET /api/strategy/functions` 支持搜索、分类、资产类型、版本过滤和详情；能列出被哪些指标/策略引用；重复或缺失函数版本启动即失败。
- `测试要求`：注册冲突、稳定排序、引用反查、弃用版本、资产过滤、序列化有限数、无副作用能力测试。
- `输出规范`：返回结构化 `id/version/displayName/category/inputs/output/pure/deprecated/supportedAssetTypes/references`，不只返回说明字符串。
- `实现增量（2026-09-05）`：策略函数详情抽屉现消费已有的精确 `referencedBy` 元数据，明确显示引用该版本函数的 Indicator 与 Strategy ID；引用关系只由注册表和本地已保存 Definition 派生，不执行策略、回测或计算行情。定向 Playwright 覆盖两类引用同时展示；未运行全量验证，保持 `VERIFYING`。
- `实现增量（2026-09-05）`：函数详情同时改为完整展示注册表 `inputs`（含必填序列输入及类型）和 `output` 返回类型，避免仅从可调参数推断函数签名。定向 Playwright 覆盖输入、返回类型以及两类引用同时可见；未运行全量验证，保持 `VERIFYING`。

### R4-T015 — 基础 Strategy Function 函数库

- `task_id`：`R4-T015`
- `title`：基础 Strategy Function 函数库
- `priority`：P0
- `执行对象`：纯函数实现、注册定义与单元测试
- `state`：VERIFYING
- `failure_count`：0
- `现状`：已有 moving average、标准差、部分布林/统计函数，但缺少统一 EMA、RSI、ATR、MACD、Keltner、Donchian、Stochastic、交叉、涨跌数量、市值与周期函数契约。
- `目标`：在唯一函数库补齐 `sma/ema/highest/lowest/stddev/rsi/atr/macd/bollinger/keltner/donchian/stochastic/crossover/crossunder/percentage_change/volume_change/true_range/up_down_count/market_cap/timeframe`。
- `影响范围`：Python 计算库、DSL 白名单、注册表、共享测试向量与 Android 可支持子集。
- `依赖关系`：依赖 R4-T014；为所有 Indicator、演示 Strategy 和回测提供唯一算法。
- `技术约束`：纯函数、显式输入、无未来数据、相同输入得到相同输出；比较运算持久化为 `gte/lte/gt/eq/lt/ne`；市值函数只适用于支持公司市值的资产；周期函数不下载或重采样行情。
- `验收标准`：正常、空、NaN、缺口、历史不足和非法参数均返回确定值或结构化不可用状态；`up_down_count(N)` 按相邻 close 返回 up/down/flat 并排除第一根不可比较值。
- `测试要求`：对照手算向量、边界窗口、参数上下限、跨周期输入、缺字段、资产禁用、无未来数据及共享算法身份测试。
- `输出规范`：每个函数公开数学定义、输入/输出 Schema、暖机长度、支持资产、错误码和版本。

### R4-T016 — Indicator Registry 与指标页面基础 UI

- `task_id`：`R4-T016`
- `title`：Indicator Registry 与指标页面基础 UI
- `priority`：P0
- `执行对象`：指标注册表、策略页“指标”Section、详情与参数/样式面板
- `state`：VERIFYING
- `failure_count`：3
- `现状`：`CHART_INDICATOR_CATALOG` 仅能显示少量目录卡片，策略页没有三个一级 Section、统一搜索筛选、收藏、来源、版本、详情操作和按需加载。
- `目标`：提供指标资源列表、分类、搜索、资产过滤、收藏、内置/自定义、创建/编辑/复制/删除、版本/启用状态和右侧详情，并建立趋势、动量、波动率、成交量、市场宽度、价格通道、统计、外部市场、高级图表、自定义分类。
- `影响范围`：`StrategyView.vue`、策略 API、浏览器持久收藏、指标定义配置和响应式样式。
- `依赖关系`：依赖 R4-T013～R4-T015；图表动作依赖 R4-T019。
- `技术约束`：指标只引用注册函数和 Plot 定义；不得读取账户/订单或产生订单；Fibonacci 只作为 `drawing_tool` 入口；静态目录按 Section 懒加载并可缓存。
- `验收标准`：首批 MA、EMA、Bollinger、Keltner、Donchian、RSI、MACD、Stochastic、ATR、Chaikin Volatility、Twiggs Volatility、RVI、Volume Profile、VIX 均有完整元数据；不兼容资产禁用并显示原因。
- `测试要求`：目录过滤、收藏恢复、详情、CRUD 权限、主/副图元数据、资产禁用、缺数据源和响应式 Playwright。
- `输出规范`：卡片与详情必须显示中英文名、分类、简介、placement、支持资产、默认参数、来源、版本、收藏和状态。
- `实现增量（2026-09-05）`：自定义 Indicator 现在是已发布 chart Indicator 的安全、版本化模板副本：服务端锁定其 `calculation_id`、精确 Strategy Function 依赖、参数名称/类型/上下界及 Plot/placement，因而不接收公式、浏览器代码、账户或订单能力。策略 API 提供创建、连续新版本编辑、复制和删除；每个版本保留创建/更新时间、来源、状态和支持资产。行情指标计算按 `calculation_id` 调用同一已发布算法，故自定义指标在详情页具备相同的暖机、缺字段、VIX/Volume Profile 特殊语义和结构化不可用结果。策略页提供新建、复制、编辑为新版本、删除入口；详情页“指标”按钮每次读取同一目录，避免自定义指标被旧缓存遗漏。定向 Python 20 项、Ruff、Vue 生产构建，以及策略→详情和自定义指标“新建→连续新版本”两项 Playwright 通过；该 UI 场景曾发现保存按钮仅引用函数而未调用，修正后复跑通过。未运行全量验证或移动端设计，保持 `VERIFYING`。

### R4-T017 — Strategy Registry 与策略页面基础 UI

- `task_id`：`R4-T017`
- `title`：Strategy Registry 与策略页面基础 UI
- `priority`：P0
- `执行对象`：策略注册表、策略 Section、详情与基础操作
- `state`：VERIFYING
- `failure_count`：0
- `现状`：已有 definitions 文件 CRUD 与简单卡片，但缺少内置/自定义来源、收藏、分类、执行模式、运行状态、版本和回测状态的统一注册表。
- `目标`：建立版本化 Strategy Registry 和策略列表/详情 UI，支持搜索、分类、收藏、来源、资产、周期、状态、模式、打开、编辑、复制、删除、回测、加载到图表、启停。
- `影响范围`：策略定义持久化、API、`StrategyView.vue`、策略元数据和浏览器缓存。
- `依赖关系`：依赖 R4-T013、R4-T014；编辑依赖 R4-T018，回测依赖 R4-T020。
- `技术约束`：内置版本只读、复制后成为 custom；删除必须精确确认；Live 默认 disabled；资源列表不读取全量行情计算数据。
- `验收标准`：卡片显示名称、说明、资产范围、周期、版本、启用、更新时间、回测状态；三个 Section 切换不整页刷新，URL 可恢复当前 Section。
- `测试要求`：CRUD、复制、收藏、筛选、内置只读、启停、Live 禁用、懒加载与旧 definitions 兼容。
- `输出规范`：API 明确 `origin/status/runMode/backtestStatus/version/createdAt/updatedAt/supportedAssetTypes`，时间为北京时间展示、ISO 持久化。
- `实现增量（2026-09-05）`：已保存策略的桌面列表补齐来源（内置/自定义/预留社区或插件）、分类、现有资产枚举、运行模式、状态与收藏筛选；行内收藏会持久化到既有浏览器收藏键。“回测”与“加载到图表”均复用唯一的当前图表隔离回测链路，前者直接进入带 Strategy Report 的详情。资源定义仍只按当前 Section 懒加载，不为筛选预先计算行情或回测。定向策略筛选/报告 Playwright 与 Vue 生产构建通过；按当前安排未运行全量验证，保持 `VERIFYING`。

### R4-T018 — Strategy Definition、Rule AST 与结构化编辑器

- `task_id`：`R4-T018`
- `title`：Strategy Definition、Rule AST 与结构化编辑器
- `priority`：P0
- `执行对象`：Strategy Schema、Rule AST 校验/迁移、Visual/Form Builder
- `state`：VERIFYING
- `failure_count`：1
- `现状`：现有 builder 只支持两层 AND/OR、少量函数与简单市值条件，并保留 `python_safe_v1` UI；无法表达完整开平仓、仓位、止损止盈、加减仓、再入场和执行设置。
- `目标`：建立可序列化、可版本化、可回测、可迁移的 Strategy Definition 与递归 `AND/OR/NOT` Rule AST，提供结构化基本信息、标的、周期、开平仓、风险、仓位和回测设置编辑器。
- `影响范围`：contracts、定义存储、API 验证、策略编辑 UI、运行时编译适配层。
- `依赖关系`：依赖 R4-T013～R4-T015；为 R4-T020、R4-T022、R4-T027～R4-T029 提供输入。
- `技术约束`：不新增 Pine Script；不执行任意 Python/JS；节点引用 `functionId + version`；AST 限制深度、节点数和类型；旧 builder/formula/DSL 只经显式适配读取。
- `验收标准`：定义至少包含 universe、baseTimeframe、entryRules、exitRules、positionSizing、stopLoss、takeProfit、pyramiding、reentry、risk、execution、backtest；非法树、循环或跨类型比较被拒绝。
- `测试要求`：递归逻辑、NOT、空组、深度/节点限制、函数参数类型、版本引用、迁移、往返序列化、无任意代码测试。
- `输出规范`：保存规范 JSON AST 和 Schema 版本；自然语言仅作说明，不作为权威执行表达。
- `实现增量（2026-09-05）`：结构化编辑器已把策略方向作为显式 `long/short` 字段写入规范 Definition；编辑既有版本时会恢复原值，缺失的旧定义保持兼容地默认做多。基础周期选择器同步当前桌面行情可用的规范周期，并补入已由行情层支持的 `4h`；契约枚举相应覆盖该值。定向 Python Schema 及结构化编辑器 Playwright 验证短仓/4h 的新建保存和短仓既有定义的重开保存，Vue 生产构建通过；按当前安排未运行全量验证，保持 `VERIFYING`。
- `实现增量（2026-09-05）`：补齐此前缺失的减仓控制：新建策略以 `scale_out={enabled,ratio_percent}` 保存，编辑器将 `0` 显式解释为关闭、`1–99` 解释为首次触及止盈时的一次性比例减仓。回测按减仓数量签发受控 `OrderIntent`，精确分摊开仓手续费/滑点、保留剩余仓位，并以 `take_profit_scale_out` 记录成交与交易；止损仍在同一根 K 线优先于止盈/减仓。字段对旧 Definition 可选，缺失时保留既有“触及止盈即全平”语义。Schema/回测 Python 19 项、编辑器 Playwright 2 项及 Vue 构建通过；未运行全量验证，保持 `VERIFYING`。

### R4-T019 — K 线 Indicator 联动与多 Pane

- `task_id`：`R4-T019`
- `title`：K 线 Indicator 联动与多 Pane
- `priority`：P0
- `执行对象`：`KLineChart.vue`、行情详情页、指标实例状态与计算 API
- `state`：VERIFYING
- `failure_count`：3
- `现状`：K 线组件只接收固定键的少量指标数组，无法按注册定义动态添加多个指标、建立独立副图或从策略页加载。
- `目标`：支持多个 Indicator Instance，主图 Overlay 与副图 Pane 动态布局；从策略页“添加到当前图表”，支持显示/隐藏、删除、参数、线宽、线型、颜色和局部重算。
- `影响范围`：KLineChart、MarketView、路由/当前图表上下文、指标计算接口与本地偏好。
- `依赖关系`：依赖 R4-T015、R4-T016；被高级指标和策略图表联动复用。
- `技术约束`：计算只读取当前图表已加载的必要行情；缺字段显示断点；参数改变不整页刷新；画线系统和现有缩放/游标行为不得回归。
- `验收标准`：MA/Bollinger/Donchian/Keltner 在主图，RSI/MACD/ATR 在独立 Pane；多个指标并存且一个失败不影响其他实例。
- `测试要求`：添加、隐藏、删除、参数/样式更新、主副图、资产过滤、缺数据、缓存、缩放同步、浅深主题和 Playwright 场景 A/B。
- `输出规范`：指标实例保存 definitionId/version/parameters/style/visible/placement；计算点与 K 线时间严格对齐，缺失为 null。
- `实现证据（2026-09-03）`：新增隔离的 Indicator Calculation 服务与版本化实例计算契约，行情 API 不再复制 MA/EMA/ATR 算法；主图支持 MA/Bollinger/Donchian/Keltner，副图支持 RSI/MACD/Stochastic/ATR，并按实例返回 Plot、对齐序列和结构化不可用原因。行情详情可从注册表添加多个实例，持久化参数/样式/可见性/placement，策略页待加载动作会打开当前图表；参数变化只重算指标，动态 Pane 与全部 X 轴缩放联动。定向 Ruff、36 项指标/行情测试、Vue 生产构建及场景 A/B Playwright 通过。浏览器验证依次暴露全屏浮层层级、测试选项定位与嵌套下拉 Teleport 层级三个问题，修复后复跑通过；完整浅深主题、全套画线回归和全量验证尚未执行，故保持 `VERIFYING`。
- `实现增量（2026-09-05）`：标的详情 K 线的“指标”按钮现只读取策略页同一 `/api/strategy/indicators` 版本化目录，并严格接收 `resourceKind=indicator` 的 `indicator.*` 定义；按当前标的 `assetType` 过滤后才可选择/创建实例，画线工具不混入下拉。面板明确标注“策略页指标库”和当前资产类型；即使绕过 UI，前端实例创建仍会拒绝资产不兼容项。定向 Playwright 断言策略目录指标可加入独立 Pane、期货专用 fixture 不会出现在股票详情中，Vue 生产构建通过；未运行全量验证，任务保持 `VERIFYING`。

### R4-T020 — 基础回测引擎与两个演示策略

- `task_id`：`R4-T020`
- `title`：基础回测引擎与两个演示策略
- `priority`：P0
- `执行对象`：桌面回测引擎、Donchian+ATR 与 MA Crossover 内置策略
- `state`：VERIFYING
- `failure_count`：0
- `现状`：现有 `strategy_dsl.backtest` 只返回布尔观察时间线，明确不模拟订单、手续费或 PnL。
- `目标`：在不改变旧扫描语义的前提下新增事件驱动回测端口，完成 Strategy Function→Strategy→OrderIntent→模拟风控/成交→Trade→Chart Marker→Report 数据链。
- `影响范围`：新回测模块、策略 API、版本化运行记录、行情读取和演示定义。
- `依赖关系`：依赖 R4-T015、R4-T018、R4-T022；报告展示依赖 R4-T021。
- `技术约束`：无未来数据；信号在当前 bar 结束后生效，成交时点/价格必须配置；手续费、滑点、数量和合约乘数显式；不写真实 Order API。
- `验收标准`：Donchian+ATR 趋势策略与 MA Crossover 全链路可运行；同一 SMA 函数同时被 MA Indicator 和 MA Strategy 引用；结果绑定策略、函数、参数和数据版本。
- `测试要求`：开平仓、止损止盈、仓位、手续费、滑点、状态恢复、多空、空数据、重复运行确定性和无未来数据。
- `输出规范`：运行记录返回 runId、版本指纹、状态、不可用原因、权益点、订单意图、成交和交易记录，不复用旧观察扫描结构冒充交易回测。
- `实现证据（2026-09-03）`：新增 `strategy_backtest.py` 确定性事件驱动回测：递归 Rule AST 只在当前 bar 收盘后判定，普通信号按定义在下一根 open/close 成交，并经 `OrderIntent → Risk Engine → Backtest Execution Adapter` 产生受控成交。回测显式记录多空、仓位规模、加减仓/再入场、手续费、滑点、合约乘数、百分比/ATR/固定止损止盈、权益/回撤、markers、fills、trades、状态快照和策略/函数/参数/数据版本指纹；结果写入本地版本指纹记录。内置 `MA Crossover` 与 `Donchian+ATR` 均为只读定义，前者精确复用 `technical.sma@1`。无未来数据、延迟成交、空数据、长短仓、成本、止损、确定性和状态恢复的定向测试通过；完整回归尚未执行，故保持 `VERIFYING`。

### R4-T021 — Strategy Report

- `task_id`：`R4-T021`
- `title`：Strategy Report
- `priority`：P0
- `执行对象`：回测指标计算、报告 API、图表下方报告面板与交易表
- `state`：VERIFYING
- `failure_count`：2
- `现状`：已有通用账户绩效函数和策略扫描历史，但没有单策略回测报告、权益曲线或完整交易记录。
- `目标`：提供总/年化收益、交易次数、胜率、盈亏比、Profit Factor、最大回撤、平均持仓、最大连赢/连亏、手续费、滑点成本、最终权益和交易明细。
- `影响范围`：回测结果模型、绩效函数复用层、API、K 线 Strategy Report 面板和导出。
- `依赖关系`：依赖 R4-T020；版本复现依赖 R4-T028。
- `技术约束`：指标定义和分母为零语义明确；报告只描述回测，不冒充实盘；时间、币种、数量、合约乘数和成本均可追溯。
- `验收标准`：图表标记与交易表逐笔一致，汇总可由交易/权益明细重算；参数改变后只重新回测并更新报告。
- `测试要求`：手算收益、回撤、PF、连赢亏、成本、无交易、未平仓、长短仓和 Playwright 场景 D。
- `输出规范`：金额返回原值与币种，比例返回数值不预格式化；交易记录含 symbol、方向、开平时间/价格、数量、手续费、PnL、PnL%、原因。
- `实现证据（2026-09-03）`：新增自包含 Strategy Report 契约与报告构建器，从回测权益、fills 和 closed trades 可重算总/年化收益、胜率、平均盈亏、Profit Factor、最大回撤、平均持仓、连赢亏、成本与最终权益；零分母、无交易和年化数值溢出返回 `null + reason`，未平仓仅单列未实现 PnL，不伪造平仓交易。回测 API 同时持久化报告，提供报告读取和 UTF-8 BOM 交易 CSV；行情全屏 K 线将同一记录的 markers、指标面板和交易表置于图表下方。场景 D 验证从策略加载、K 线标记、报告、交易 CSV 到参数修改只重跑回测而不重拉行情。定向 Python、Ruff、Vue 构建和相关 Playwright 通过；先后修正浮点断言与前端按需去尾零的测试期望后复跑通过，`failure_count=2`。完整回归尚未执行，故保持 `VERIFYING`。

### R4-T022 — Order API 权限边界与运行模式门控

- `task_id`：`R4-T022`
- `title`：Order API 权限边界与运行模式门控
- `priority`：P0
- `执行对象`：能力上下文、OrderIntent、Risk Engine 端口、Execution Adapter 端口与 API 门控
- `state`：VERIFYING
- `failure_count`：1
- `现状`：当前项目没有真实 Order API；现有策略运行只产生观察信号，尚无可验证的跨层能力隔离模型。
- `目标`：在后端强制 Indicator×Order、Function×Order、Strategy✓OrderIntent；所有意图先过 Risk Engine，再由隔离适配器执行；Backtest/Paper/Live 能力分离。
- `影响范围`：策略运行上下文、回测模拟端口、未来券商适配边界、API 与 UI 状态。
- `依赖关系`：依赖 R4-T013、R4-T018；R4-T020 使用回测适配器；真实 Live 另行立项。
- `技术约束`：无已验收执行适配器时 Live 永久返回 DISABLED；前端隐藏按钮不能替代后端授权；Android 不产生 OrderIntent；日志不得记录凭据。
- `验收标准`：任何 Indicator/Function 伪造或调用订单能力均被拒绝；只有合法 Strategy Definition 可生成意图，且不能绕过风险端口直达执行。
- `测试要求`：权限矩阵、伪造 resourceType、绕过风控、模式混用、Live 禁用、Paper 隔离、幂等 intentId 与审计日志测试。
- `输出规范`：明确返回 capability、runMode、riskDecision、executionStatus、reason；禁用态使用 409/422 契约或只读状态，不返回伪成功。
- `实现证据（2026-09-03）`：新增自包含 OrderIntent/Execution Result Schema、服务端 Capability Context、定义风控端口、不可绕过的风险许可、Backtest/Paper 隔离模拟适配器、精确版本加载、幂等 intentId 存储与允许字段审计；Indicator、Strategy Function、Android、伪造 resourceType、定义/版本变化和运行模式混用均被后端拒绝，Live 能力只读返回 DISABLED 且写请求返回 409。策略页展示真实能力 API 状态。定向 Ruff 与 8 项模块/API 测试通过；首次测试暴露相对 Schema 引用会尝试远程解析，改为自包含后通过。完整回归尚未执行，故保持 `VERIFYING`。

### R4-T023 — 高级技术 Indicator 套件

- `task_id`：`R4-T023`
- `title`：高级技术 Indicator 套件
- `priority`：P1
- `执行对象`：Chaikin Volatility、Twiggs Volatility、RVI 等指标定义与 Plot
- `state`：VERIFYING
- `failure_count`：1
- `现状`：首批常用技术指标和函数将在 P0 建立，高级波动指标尚无经过定义审计的实现。
- `目标`：补齐高级指标定义、参数、暖机、主副图位置、帮助说明和资产适用性，并复用基础函数或发布显式新函数版本。
- `影响范围`：函数/指标注册表、指标计算 API、指标 UI 与 K 线 Pane。
- `依赖关系`：依赖 R4-T015、R4-T016、R4-T019。
- `技术约束`：公式来源和变体必须明确；不同定义不得共用同一版本 ID；无成交量资产禁用依赖成交量的指标。
- `验收标准`：三个高级指标可搜索、配置、添加、更新和删除，结果对照固定向量。
- `测试要求`：公式向量、暖机、缺字段、参数边界、资产过滤和图表回归。
- `输出规范`：说明页列出算法版本、公式、所需字段、单位、暖机长度和限制。
- `实现证据（2026-09-03）`：新增三项可被 Strategy/Indicator 共同引用的纯函数：Chaikin 为 `100 × (EMA(H-L)[t] / EMA(H-L)[t-m] − 1)`，RVI 明确采用 Dorsey 1993 收盘价版本（滚动标准差按涨跌日拆分后 Wilder 平滑）；两者均有公开公式来源、所需字段、单位、暖机和限制。Twiggs® 原始算法属于专有实现且未公开完整公式，故不伪称复现：注册为显式“ATR% 公开变体”，公式为 `100 × Wilder ATR / close`，同时在详情页给出该限制。三个指标均为 active 副图，使用精确版本函数、固定向量、无未来数据、参数边界、缺字段、资产过滤和动态 Pane 测试；菜单添加/更新/删除及调参不重拉行情窗口的 Playwright 回归通过。首次场景回归发现管理器展开后第二个样式下拉框令测试定位不唯一，改为精确选择添加指标控件后复跑通过，`failure_count=1`。完整回归尚未执行，故保持 `VERIFYING`。

### R4-T024 — Volume Profile 高级图表

- `task_id`：`R4-T024`
- `title`：Volume Profile 高级图表
- `priority`：P1
- `执行对象`：价格分桶函数、Volume Profile Plot 与交互
- `state`：VERIFYING
- `failure_count`：1
- `现状`：现有 K 线没有按价格区间聚合成交量的横向分布图，也没有可追溯的分桶口径。
- `目标`：以当前可见或指定区间生成可配置分桶的成交量分布、POC 与价值区，并在主图右侧绘制。
- `影响范围`：函数注册表、指标注册表、KLineChart 图层、缩放联动和性能。
- `依赖关系`：依赖 R4-T019、R4-T023。
- `技术约束`：成交量字段缺失则不可用；分桶、价格代表值、价值区比例和可见区间语义版本化；不由 amount 伪造 volume。
- `验收标准`：缩放后按配置重算且不会冻结；分桶量之和与纳入 K 线成交量一致。
- `测试要求`：边界价格、空量、分桶恒等式、缩放、性能、主题和截图回归。
- `输出规范`：返回 bucketLow/bucketHigh/volume/share/isPoc/isValueArea 与范围元数据。
- `实现证据（2026-09-05）`：新增版本化 `market.volume_profile@1` / `indicator.volume_profile@1`。它只接受指定可见闭区间内每根 K 线的 `high/low/close/volume`，以 HLC3 映射至等宽价格桶，整根成交量只归入一个桶；不读取或以 `amount` 补造 `volume`。响应显式给出算法版本、HLC3 代表价格、完整归集口径、请求/有效桶数、纳入 bar 范围及 POC/价值区（从 POC 向相邻桶成交量扩张至 70%，并在并列时优先低价桶）。常量价格退化为单桶，任何缺失/空成交量均以结构化不可用原因返回。主图右侧仅绘制与当前可见范围精确匹配的 profile，缩放/滚轮变更会经 120ms 防抖重新请求该范围，旧范围结果不会冻结或误绘。固定向量覆盖桶边界、POC、价值区、守恒、范围裁剪、缺失/空量及 5,000 根/200 桶性能；API 覆盖范围元数据及守恒；Playwright 覆盖添加、缩放重算与删除。定向 Ruff、57 项 Python 测试、Vue 构建和该浏览器场景均通过。首次定向测试曾遗漏 `pytest` 导入，补正后复跑通过，`failure_count=1`；全量回归、主题截图及人工视觉验收尚未执行，故保持 `VERIFYING`。

### R4-T025 — Fibonacci Retracement 画线工具

- `task_id`：`R4-T025`
- `title`：Fibonacci Retracement 画线工具
- `priority`：P1
- `执行对象`：画线模型、KLineChart 交互、策略页 drawing_tool 入口
- `state`：VERIFYING
- `failure_count`：5
- `现状`：画线已有水平、垂直、矩形、文本和笔刷，尚无斐波那契回撤；它不应进入普通技术指标计算结构。
- `目标`：增加 `type=fibonacci_retracement` 双锚点画线，展示标准可配置比例、价格标签、样式和持久化，并在指标页以绘图工具入口呈现。
- `影响范围`：画线 Schema/API、KLineChart、DrawingColorPicker、指标目录和偏好。
- `依赖关系`：依赖 R4-T016、现有 R4-T002/R4-T003 画线基线。
- `技术约束`：逻辑时间/价格坐标、无指标函数依赖、至少两点、比例有限且有数量上限；兼容历史画线文档。
- `验收标准`：创建、拖动两端、整体移动、锁定、跨周期、颜色/线型/透明度、删除和刷新恢复可用。
- `测试要求`：Schema、价格计算、反向锚点、比例参数、旧文档兼容、鼠标/触摸与 Playwright。
- `输出规范`：保存 anchors、levels、labels、style、version；不保存屏幕像素或伪 Indicator 数据。
- `实现证据（2026-09-05）`：新增独立 `drawing.fibonacci_retracement@1` 资源（`resourceKind=drawing_tool`），在策略页“指标”目录以“打开绘图工具”入口呈现；它没有 Strategy Function 依赖，不能被 Indicator Instance 或策略规则调用。画线文档以两个逻辑 `time/price` 锚点、`version=1`、最多 16 个 `{ratio,label}`、样式和跨周期设置保存；0% 固定为第二锚点、100% 为第一锚点，价格公式为 `anchorEnd + (anchorStart − anchorEnd) × ratio`，因此反向锚点保持用户所画的波段方向。KLineChart 支持预览、双击点创建、两端独立拖动、整体移动、锁定、颜色/线宽/线型、比例编辑、跨周期、删除及刷新恢复；没有保存屏幕像素。画线异步读取现在带本地编辑修订号，迟到的旧 GET 不会覆盖刚创建或修改的本地快照。纯函数/API 测试覆盖标准比例、反向锚点、有限比例/锚点、版本、数量上限和旧文档兼容；Playwright 覆盖创建、端点拖动、整体移动、比例修改、锁定、跨周期切换、刷新恢复及删除。定向 Ruff、62 项 Python 测试、Vue 构建和该浏览器场景通过。构建首次因 `<script setup>` 导出运行时常量失败，随后四次浏览器测试分别暴露静态构建产物未刷新、异步保存匹配与默认跨周期断言问题，均修正后通过，`failure_count=5`。触摸专用与浅/深主题截图回归尚未执行，故保持 `VERIFYING`。

### R4-T026 — VIX External Market Indicator

- `task_id`：`R4-T026`
- `title`：VIX External Market Indicator
- `priority`：P1
- `执行对象`：外部市场指标定义、CBOE 受控同步、数据能力门、跨序列对齐与副图
- `state`：VERIFYING
- `failure_count`：3
- `现状`：VIX 不能由当前标的自身 OHLC 推导。CBOE 官方历史 CSV 已经单独探测并可通过 `vix-sync` 生成本地标准序列；图表仍须离线读取、有源才启用。
- `目标`：把 VIX 注册为 External Market Indicator；有可追溯本地标准序列时按交易日对齐显示，否则禁用并显示“缺少数据源”。
- `影响范围`：Provider 能力、标准标的映射、指标注册表、计算接口和图表。
- `依赖关系`：依赖 R4-T016、R4-T019；真实启用依赖独立来源探针与受控本地同步。
- `技术约束`：页面不直连第三方；不得从任意标的 K 线合成 VIX；美中交易日差异产生 null 断点，不前值填充。
- `验收标准`：无源时禁用且原因清晰；有 fixture/真实 PASS 源时正确对齐、展示来源和数据日期。
- `测试要求`：缺源、部分日期、时区/交易日、资产切换、来源失败与禁止造数测试。
- `输出规范`：返回 externalInstrumentId、source、asOfDate、coverage、points、status/reason。
- `实现证据（2026-09-05）`：新增 `external_market.py` 的只读本地标准序列门，唯一候选路径为数据根目录下 `external_market/vix/series.json`。它要求 `schemaVersion=1`、固定 `US.CBOE.INDEX.VIX` 映射、非空 `source`、`sourceStatus=PASS`、唯一有限交易日点以及与最新点一致的 `asOfDate`；任何缺失、失败或格式问题均返回 `MISSING_DATASOURCE`，不请求网页第三方、也不从当前标的 OHLC 造数。通过门的序列仅按 `tradingDate` 精确连接，交易所/时区日期差异和缺口均为 `null`，绝不前值填充；无当前区间交集返回 `NO_ALIGNED_POINTS`。实例响应返回来源、截至日、覆盖、精确匹配点和状态，行情页以普通副图渲染并在指标管理器展示来源/数据截至日/对齐数。fixture 覆盖缺源、来源 `FAILED`、部分交易日、带时区 bar、资产切换、无交集和禁止造数测试。现新增受控 `vix-sync`：只抓取 CBOE 官方 `VIX_History.csv`，严格拒绝缺 DATE/CLOSE、非法/重复交易日与非有限收盘价，并把来源 URL、SHA-256、取得时间、PASS 与全量点一次原子写入；图表进程始终只读本地文件。2026-09-05 官方端点实测 HTTP 206，通过命令写入 9,266 点，截至 2026-09-04，真实 `load_vix_series` 返回 ready；东方财富 DXY 不因该结果提升为已验证。定向外部序列/同步/指标/API pytest 11 项与 Ruff 通过；命令与真实来源同步通过。首次同步单测发现 CLI 的摘要有两行 JSON、并发现一个未使用导入，均修正，`failure_count=3`。未运行全量验证或移动端设计，故保持 `VERIFYING`。

### R4-T027 — Strategy Visual Builder 增强

- `task_id`：`R4-T027`
- `title`：Strategy Visual Builder 增强
- `priority`：P1
- `执行对象`：条件树交互、函数选择器、仓位与风险表单
- `state`：VERIFYING
- `failure_count`：3
- `现状`：P0 编辑器完成结构化表单后，复杂嵌套、拖动排序、引用预览和局部校验仍需增强。
- `目标`：提供递归 AND/OR/NOT 可视树、函数签名驱动参数控件、依赖预览、节点复制/排序、错误定位及仓位/止损/止盈可视编辑。
- `影响范围`：StrategyView 子组件、表单状态、AST API 和可访问性。
- `依赖关系`：依赖 R4-T018、R4-T020。
- `技术约束`：UI 只生成规范 AST，不生成可执行字符串；所有选项来自注册表，不在前端写死第二份函数目录。
- `验收标准`：复杂示例树可无损编辑、保存、重开和回测；错误精确定位至节点；键盘可完成主要操作。
- `测试要求`：嵌套树、排序、复制、撤销边界、参数联动、资产禁用、键盘与窄屏 Playwright。
- `输出规范`：编辑器状态最终规范化为 AST；临时 UI ID 不进入权威定义。
- `实现证据（2026-09-05）`：结构化策略编辑器已从固定“均线/阈值”模板改为递归 `StrategyRuleTreeEditor`。开仓和平仓各保存一棵内存 camelCase AST 树，支持任意已发布的 AND/OR/NOT、条件、嵌套函数操作数、节点复制、按钮及 HTML5 拖动排序和 50 步撤销；保存时唯一转换为 Schema 所需 snake_case AST，不写入临时节点 ID 或可执行字符串。函数下拉、每个参数的期望类型、默认值、嵌套函数候选和资产适用性全部来自 `/api/strategy/functions` 注册表；本地校验按路径显示函数不存在、签名数量、NOT 子数、数值比较与资产不适配错误，服务端仍按精确函数版本作最终 AST 校验。编辑器显示依赖预览，并可编辑 `fixed_quantity/equity_percent/risk_percent` 仓位、百分比/ATR 倍数/固定价格止损止盈、最大回撤、加仓与再入场；固定价格不再被百分比 100 上限截断。复杂的已发布 AST 打开后直接保真重建，保存为下一版本而不是降级成不可编辑模板。窄屏 Playwright 覆盖嵌套 OR/NOT、复制、排序、撤销、键盘选择、资产错误定位、依赖、规范 AST 提交，以及复杂 AST 重开/回存和非默认仓位/风险设置；Python 定义/API 24 项与 Ruff、Vue 构建、两项定向 Playwright 均通过。首次构建暴露操作数联合类型的模板缩窄错误，随后一次验证按钮未实际调用异步校验，复杂策略回存又发现固定价格被 `%` 上限截断；三项均修复，`failure_count=3`。尚未执行完整 Playwright、全量 pytest、主题/真实浏览器手感、真实复杂策略回测，故保持 `VERIFYING`。

### R4-T028 — 版本管理与历史策略复现

- `task_id`：`R4-T028`
- `title`：版本管理与历史策略复现
- `priority`：P1
- `执行对象`：不可变资源版本、迁移、版本选择与回测复现
- `state`：VERIFYING
- `failure_count`：1
- `现状`：现有定义以文件覆盖更新为主，Function/Indicator/Strategy 的历史版本和引用锁定不足。
- `目标`：将已发布版本设为不可变，编辑生成新版本；回测记录锁定函数、指标、策略、参数和数据版本，可选择历史版本复现。
- `影响范围`：资源存储、注册表、API、策略/函数详情和回测记录。
- `依赖关系`：依赖 R4-T014、R4-T017、R4-T020、R4-T021。
- `技术约束`：版本算法改变必须递增；旧版本不得原地重写；删除仅停用/归档被引用版本；迁移可回滚。
- `验收标准`：`technical.atr@1` 与 `@2` 可并存，旧策略继续解析 `@1` 并得到同一 fixture 结果。
- `测试要求`：不可变写、并存、引用锁定、弃用、迁移、回滚、历史复现哈希和缺失版本错误。
- `输出规范`：每次运行保存完整 dependencyLock 和 definitionHash，API 不用“latest”替代历史引用。
- `实现证据（2026-09-05）`：`StrategyFunctionRegistry` 现同时发布 `technical.atr@1` 与 `technical.atr@2`，并让 Rule AST 的精确 `version` 传入函数执行器；v1 仍以原 `atr` 运行时解析，v2 使用独立 `atr_v2` 运行时身份，历史 ATR fixture 不因 v2 发布而变化。结构化策略资源继续以 `id@version.json` 顺序写入，不允许同版本覆盖；删除请求改为创建 `deprecated` 归档版本，返回引用它的回测运行，不物理删除已发布定义。旧的未版本化资源可通过受控迁移端点预检后移入版本化文件和迁移专属备份，回滚仅在没有回测引用时恢复原文件。

  每个新回测结果及 Report 现有 `definitionHash` 与完整 `dependencyLock`：策略/每个函数的 `id/version/definitionHash`、显式空的 `indicators`（Rule AST 不能执行图表指标）、参数值及哈希、行情数据版本/哈希/窗口、引擎版本；版本指纹由该锁、乘数和币种计算。预 T028 的旧回测仍可读取原报告，但其 `definitionHash/dependencyLock` 明确为 `null`，不能进入复现入口。`POST /api/strategy/backtests/{runId}/reproduce` 只按锁内版本解析策略和函数，先校验各定义哈希，再以原查询窗口重跑；缺定义、缺函数、定义变化或行情变化分别返回结构化 409，绝不以 latest 回退或伪造结果。策略页可打开全量历史版本并选择“用于回测”或“编辑为新版本”，行情报告显示依赖锁并提供“复现此版本”。定向 43 项 Python（含不可变写、ATR 双版本、函数/策略锁、归档引用、迁移/回滚、复现哈希、缺失版本与旧报告兼容）、Ruff、Vue 构建及 3 项 Playwright 通过；专项报告测试首次发现旧记录缺少新字段，改为显式 `null` 后复跑通过，`failure_count=1`。全量回归、真实历史迁移和主题/手感验收未执行，故保持 `VERIFYING`。

### R4-T029 — 策略导入导出

- `task_id`：`R4-T029`
- `title`：策略导入导出
- `priority`：P2
- `执行对象`：签名/校验包、导入预览、冲突处理与导出 UI
- `state`：VERIFYING
- `failure_count`：1
- `现状`：Android 有签名 Strategy DSL 包契约，桌面自定义 Strategy/Indicator/Function 缺少统一可移植包。
- `目标`：导出结构化资源、依赖锁和测试向量；导入前只读验证 Schema、版本、权限与冲突，禁止携带任意代码。
- `影响范围`：contracts、桌面包工具、策略页、Android 可接受子集说明。
- `依赖关系`：依赖 R4-T028、ADR-0008；不扩大 Android 执行节点白名单。
- `技术约束`：不导出凭据、个人账户、运行数据或真实订单；未签名或未知节点包不能进入 Android；桌面自定义资源仍受同一白名单。
- `验收标准`：同版本往返无损，冲突需显式改名/新版本/取消；恶意路径、超大包、任意代码和未知能力被拒绝。
- `测试要求`：往返、签名、篡改、冲突、路径穿越、大小限制、依赖缺失和兼容矩阵。
- `输出规范`：包含 manifest、definitions、dependencyLock、schemas、testVectors 和校验摘要。
- `实现证据（2026-09-05）`：新增只面向桌面 Rule AST 的 `strategy-transfer` ZIP：固定包含 `manifest`、结构化 Definition、精确 Strategy Function dependency lock、测试向量和两份权威 Schema，各内容有 SHA-256 摘要；导入前在内存中拒绝重复/未知 ZIP 项、绝对或穿越路径、压缩炸弹总解压尺寸、篡改哈希、替换 Schema、任意字段/能力、未知函数版本和无效 JSON。包可选 Ed25519 签名，签名包含公开键指纹并在导入预览时校验；未签名包仍只能作为同一白名单下的桌面受限资源。该格式固定 `target=desktop`，无论签名与否都不能进入 Android；Android 继续仅接受 ADR-0008 的本人签名声明式 DSL 包。策略页支持自定义结构化策略导出，导入时先展示策略、函数锁、签名、Android 边界和版本冲突；冲突只能显式取消、改名或创建连续新版本，绝不覆盖。定向 31 项 Python（往返、签名/篡改、路径、压缩包总解压尺寸、任意代码、未知依赖、API 冲突和无损写入）、Ruff、Vue 构建及 Playwright 导入场景通过；浏览器场景首次受 Element Plus radio 内层命中拦截而超时，改为点击标签后复跑通过，`failure_count=1`。未运行完整回归、外部发布密钥配置、真实跨设备 Android DSL 导入或主题/手感检查，故保持 `VERIFYING`。

### R4-T030 — 策略模板

- `task_id`：`R4-T030`
- `title`：策略模板
- `priority`：P2
- `执行对象`：内置模板目录、创建向导与参数预览
- `state`：VERIFYING
- `failure_count`：0
- `现状`：用户只能从空白或复制现有定义开始，缺少受控的趋势、突破、反转和风险模板。
- `目标`：以不可变 builtin 模板提供新建起点，复制后形成 custom Strategy，并保留来源模板/version。
- `影响范围`：策略注册表、创建 UI、文档和示例数据。
- `依赖关系`：依赖 R4-T017、R4-T018、R4-T020、R4-T028。
- `技术约束`：模板不是运行中的共享可变定义；不得宣称盈利；默认参数需明确仅用于演示。
- `验收标准`：至少提供 MA Crossover、Donchian+ATR 及空白模板，预览依赖、资产和风险后方可创建。
- `测试要求`：模板复制隔离、版本来源、默认参数、缺失依赖、内置只读和创建流程。
- `输出规范`：模板公开 templateId/version/sourceStrategyVersion/defaultOverrides/disclaimer。
- `实现证据（2026-09-05）`：新增不可变 `template.ma_crossover@1`、`template.donchian_atr@1` 与 `template.blank@1` 目录。前两项精确引用内置 Strategy 版本；空白项使用永不触发的同序列交叉作为 Schema 合法占位规则，提示用户在编辑器中先配置规则，绝不保存空或任意代码 AST。模板 API 返回来源版本、资产、函数依赖、参数/仓位/止损/最大回撤预览和无收益承诺声明；选择模板只会创建带 `templateSource` 溯源的独立 custom `v1`，不改变 builtin。策略页“从模板创建”向导先展示这些信息再创建并打开结构化编辑器。定向 16 项 Python、Ruff、Vue 构建及模板 Playwright 通过；未运行完整回归或人工参数/主题检查，故保持 `VERIFYING`。

### R4-T031 — 策略分享/社区能力预留

- `task_id`：`R4-T031`
- `title`：策略分享与社区能力预留
- `priority`：P2
- `执行对象`：资源来源枚举、权限/信任元数据和扩展接口设计
- `state`：VERIFYING
- `failure_count`：0
- `现状`：本轮只支持 builtin/custom；未来插件或社区资源若直接复用本地信任级别会扩大执行风险。
- `目标`：仅预留 `community/plugin` 来源、发布者、签名、信任和审核状态契约，不开发社区、联网分享、排名或下载服务。
- `影响范围`：资源 Schema、注册表过滤、导入 UI 和安全文档。
- `依赖关系`：依赖 R4-T029；真正社区功能须另立计划并评审来源授权、隐私和执行安全。
- `技术约束`：预留枚举不得自动获得执行权限；未信任来源默认 disabled；不引入云服务或用户账户依赖。
- `验收标准`：builtin/custom 行为不变；community/plugin fixture 可被识别但不可执行，并显示需审核原因。
- `测试要求`：未知来源、信任升级、权限默认值、向后兼容和无网络请求测试。
- `输出规范`：只输出扩展字段和安全说明，不提供伪社区页面或模拟排名。
- `实现证据（2026-09-05）`：Strategy Definition 契约现在预留 `origin=community|plugin` 与 `trustMetadata`（发布者、可选 Ed25519 签名键指纹/验证状态、`trustState`、`reviewStatus`）。这两类来源当前只能以 `status=disabled` 存储和展示；即使 fixture 的信任状态为 `trusted`，服务端仍拒绝其回测、订单意图、复制、状态升级和删除，页面明确标注“社区/插件 · 待审核”并禁用图表/编辑/复制入口。没有新增联网、账户、下载、排名或发布 API；T029 也仍只导入 custom 包。定向 12 项 Python、Ruff、Vue 构建与来源只读 Playwright 通过；未运行完整回归或实施未来审核/信任升级流程，故保持 `VERIFYING`。

## 策略系统推荐执行顺序

1. `R4-T013 → R4-T014 → R4-T015`：先锁定三层契约、注册表与唯一基础函数实现。
2. `R4-T016 → R4-T017 → R4-T018`：完成三个策略页面 Section、资源管理和结构化 Strategy Definition。
3. `R4-T019 → R4-T022 → R4-T020 → R4-T021`：打通图表、权限端口、回测和报告；R4 初期一个图表最多一个 Strategy。
4. `R4-T023 → R4-T024 → R4-T025 → R4-T026 → R4-T027 → R4-T028`：按函数、图表工具、外部数据能力和版本复现逐项验收 P1。
5. `R4-T029 → R4-T030 → R4-T031`：最后实现可移植包、模板和只读扩展预留；本轮不建设策略社区。
6. 每项按 `PLAN_CREATED → CODING → CODE_REVIEW → TESTING → VERIFYING → DONE` 推进；缺来源或执行适配器时保持 `BLOCKED/DISABLED`，不得用 fixture 冒充真实能力。

## R3 未完成项承接

- R3-T002 的资产类型 Schema 枚举差异继续保留，后续归入 R4 数据契约任务。
- R3-T008 已由 R4-T011 的 `tdx-cn-v2` 代码和迁移接管；完成真实来源替换与全量验收后在本任务封板，不再把历史统一 `/100` 结论当作当前实现。
- R3-T009 已终止：TickDB 从未形成 Silver，本地目录和活动下载代码均已移除；未来若重新引入须从来源授权、Raw 下载和单位验证重新立项。
- R3 中尚处 `VERIFYING` 的真实大库、长时间运行和人工手感验证继续作为历史未完成证据，不因 R4 开启而改写为完成。

## R4-T001 推荐执行顺序

1. 用户锁定合约/品种热度粒度、合成公式、五日精确权重、标签阈值、覆盖门槛和 K 线/多轴/基差口径；四张结构图的专项口径转由 `R4-T004～R4-T007` 维护。
2. 固化六个新数据集的字段、主键、来源、单位与交易日语义，补齐至少 6 个交易日的确定性样本。
3. 对六个国内交易场所分别执行月份合约、席位排名和现货/基差真实能力探针，保留 `PASS / FAILED / UNSUPPORTED` 证据。
4. 把固定螺纹钢龙虎榜样例改为真实全合约增量采集，生产全月份合约快照、席位明细和来源覆盖报告。
5. 计算品种/席位市值与持仓分布、五日热度、加权合约序列、席位四方向持仓和可用基差。
6. 实现 FastAPI 契约与 API 测试，再新增 `/futures/` 页面、顶部导航和七个可独立降级的模块。
7. 完成单元、API、真实探针、Vue、Playwright、大系列性能、全量回归及文档验收。

## R4-T002 推荐执行顺序

1. 抽离并冻结 80 色有序常量，先更新色板数量、顺序和持久化兼容测试。
2. 调整 `DrawingColorPicker.vue` 为 10×8 预设调色板，并实现附图中的当前颜色、自定义颜色入口和透明度布局。
3. 接入线条/文字色与箱体填充色，验证 RGB 与 Alpha 独立更新且不破坏各图形类型的永久偏好。
4. 完成键盘、点击外部收起、浅色/深色主题、删除重建和浏览器重启回归，再同步状态、日志和交付证据。

## R4-T003 推荐执行顺序

1. 先扩展 `brush` 类型、独立默认样式和后端边界验证，用混合旧画线 JSON 的契约测试锁定“无迁移”。
2. 实现 pointer capture、路径预览、采样/简化/点数上限和收笔一次提交，先通过长路径性能用例。
3. 实现折线渲染、命中选择、整体拖动、锁定和跨周期投影，并在卡片图中保持只读。
4. 复用 R4-T002 的颜色选择器，接入粗细、线型、锁定、跨周期、删除和独立偏好持久化。
5. 完成 Playwright、浅色/深色主题、鼠标/触摸、刷新持久化和人工手感验收，再同步状态、日志和交付证据。

## R4-T004～R4-T007 推荐执行顺序

1. 审计有效月份合约、价格字段、合约乘数和席位排名覆盖，锁定名义持仓规模的单一 `priceBasis`，保存至少一个完整真实交易日的逐合约核对表。
2. 固化 `FUTURES_STRUCTURE_DAILY` 与 `FUTURES_STRUCTURE_BASELINE` 契约；先用确定性夹具验证四类聚合、单边不乘二、方向口径、合计恒等式、固定排序和固定“其他”集合。
3. 实现品种日度结构，再把固定螺纹钢样例替换为真实全合约席位采集，生产席位日度结构及分交易所覆盖报告。
4. 实现结构 API、主图、固定“其他”下钻、颜色和悬浮联动；每个图表、指标和方向独立读取持久化基准元数据。
5. 完成约 100 品种、500 席位及全历史日线性能验证、真实合理区间核对、Playwright、全量回归和文档交付；未通过真实席位覆盖的任务保持 `ANALYSIS` 或 `VERIFYING`，不得标记 `DONE`。
